---
phase: 03-application-pages
plan: '03'
type: execute
wave: 2
depends_on:
  - '03-01'
files_modified:
  - apps/desktop-ui/src/routes/(app)/admin/+page.svelte
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Admin dashboard renders at /admin"
    - "Shows placeholder chart area (simulated bar/line charts with gray blocks)"
    - "Shows statistics cards"
    - "Mobile: cards stacked vertically, full width"
    - "Desktop: 2-3 column card grid"
    - "Sidebar nav highlights when on admin page"
  artifacts:
    - path: "apps/desktop-ui/src/routes/(app)/admin/+page.svelte"
      provides: "Admin dashboard placeholder with charts and stats"
      min_lines: 50
  key_links:
    - from: "(app)/admin/+page.svelte"
      to: "(app)/+layout.svelte"
      via: "Renders within app layout, active nav state in layout"
---

<objective>
Create Admin dashboard placeholder at /admin with chart placeholders (gray gradient blocks simulating bar/line charts) and statistics cards. Responsive layout adapts to mobile and desktop.

Purpose: Placeholder dashboard showcasing responsive grid layout. Real data integration deferred to future phases.
Output: Admin page with chart placeholders and stat cards
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-application-pages/03-CONTEXT.md §Admin Dashboard Page

# From context (design decisions):
- D-22: Placeholder content: chart placeholder (gray blocks simulating bar/line charts) + statistics cards
- D-23: Mobile: list layout (cards stacked vertically, full width)
- D-24: Desktop: 2-3 column card grid
- D-25: Sidebar navigation item highlighted when on admin page

# Note from D-25: Active nav highlighting is handled in the (app)/+layout.svelte from Plan 1 (uses $page.url.pathname)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Admin dashboard placeholder page</name>
  <files>
    - apps/desktop-ui/src/routes/(app)/admin/+page.svelte
  </files>
  <read_first>
    - apps/desktop-ui/src/routes/(app)/+layout.svelte (verify navigation structure)
    - apps/desktop-ui/src/lib/components/index.ts (for Card, Badge components)
  </read_first>
  <action>
Create apps/desktop-ui/src/routes/(app)/admin/+page.svelte:

1. Page layout:
   - Standard page padding: p-4 md:p-6
   - Page title at top: "Admin Dashboard" in h1 text-2xl

2. Statistics Cards Row:
   - Use 4 placeholder stat cards (can expand to more later):
     - "Total Users": placeholder number
     - "Active Sessions": placeholder number
     - "Revenue": placeholder number  
     - "Growth": percentage placeholder
   - Each in a Card component from $lib/components
   - Stats number: text-2xl font-semibold
   - Stats label: text-sm text-muted

3. Chart Placeholders:
   - 2 chart areas: "Revenue Over Time" and "User Activity"
   - Each is a Card containing gray gradient blocks that simulate bar/line charts
   - Use divs with varying gray backgrounds to create pseudo-chart visual:
     - Bar chart: vertical rectangles with heights representing data
     - Use variants of gray-200, gray-300, gray-400 for visual depth
   - Chart area should look like placeholder data (abstract, not real charts)

4. Responsive Layout:
   - Mobile (< md/800px): 
     - Stats cards: 1 column, full width, stacked
     - Charts: full width, stacked vertically
   - Desktop (>= md/800px):
     - Stats cards: grid cols-4 or flex-wrap
     - Charts: 2 columns side by side

5. Styling:
   - Use theme tokens: primary-*, gray-*, var(--color-bg), var(--color-bg-elevated)
   - Subtle shadows on cards: shadow-sm
   - Generous padding and whitespace per Linear aesthetic

Use Card, Badge from $lib/components. No external charting library - pure CSS placeholders.
  </action>
  <acceptance_criteria>
- File exists at: apps/desktop-ui/src/routes/(app)/admin/+page.svelte
- Contains "Admin Dashboard" page title
- Shows 4 statistics cards with placeholder numbers
- Shows 2 chart placeholder areas with gray gradient blocks
- Mobile: single column, cards stacked
- Desktop: multiple columns, grid layout
- Uses Card component from $lib/components
- Uses Badge component where appropriate
  </acceptance_criteria>
  <verify>
    <automated>ls "apps/desktop-ui/src/routes/(app)/admin/+page.svelte" && grep -q "Total Users" "apps/desktop-ui/src/routes/(app)/admin/+page.svelte" && grep -q "grid-cols-" "apps/desktop-ui/src/routes/(app)/admin/+page.svelte" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Admin dashboard placeholder page created with stats cards and chart areas</done>
</task>

</tasks>

<verification>
- [ ] /admin renders with page title
- [ ] 4 stat cards visible with placeholder numbers
- [ ] 2 chart placeholder areas visible
- [ ] Mobile: stacked layout
- [ ] Desktop: grid layout with multiple columns
</verification>

<success_criteria>
User can see admin dashboard at /admin with placeholder content - statistics cards and chart areas - properly laid out responsively.
</success_criteria>

<output>
After completion, create `.planning/phases/03-application-pages/03-03-SUMMARY.md`
</output>