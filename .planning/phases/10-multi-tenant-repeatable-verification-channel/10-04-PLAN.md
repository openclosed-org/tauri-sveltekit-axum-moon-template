---
phase: 10-multi-tenant-repeatable-verification-channel
plan: 04
type: execute
wave: 4
depends_on: ["02"]
files_modified:
  - packages/core/usecases/src/counter_service.rs
  - servers/api/src/routes/counter.rs
  - servers/api/tests/http_e2e_test.rs
  - .planning/phases/10-multi-tenant-repeatable-verification-channel/10-04-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [MTEN-02]
must_haves:
  truths:
    - "Counter mutations in tenant-1 do not alter tenant-2 values"
    - "Counter isolation remains true across repeated runs"
    - "API counter handlers consume middleware tenant identity"
  artifacts:
    - path: "packages/core/usecases/src/counter_service.rs"
      provides: "Tenant-scoped counter read/write queries and schema migration"
    - path: "servers/api/src/routes/counter.rs"
      provides: "Counter endpoints wired to TenantId from request extensions"
    - path: "servers/api/tests/http_e2e_test.rs"
      provides: "Cross-tenant counter isolation regression tests"
  key_links:
    - from: "servers/api/src/middleware/tenant.rs"
      to: "servers/api/src/routes/counter.rs"
      via: "Request extensions TenantId extraction"
      pattern: "extensions\(\).*TenantId"
    - from: "servers/api/src/routes/counter.rs"
      to: "packages/core/usecases/src/counter_service.rs"
      via: "tenant-aware counter service method calls"
      pattern: "for_tenant|tenant_id"
---

<objective>
修复 Phase 10 验证失败的核心断点：把 API counter 读写路径改为真正 tenant-scoped，消除当前全局 `id = 1` 导致的跨租户串值。

Purpose: 闭合 MTEN-02 的真实数据流缺口，让 Web/Desktop 隔离断言对应真实后端行为，而不是“测试通过但后端共享状态”的假阳性；并明确闭合 `10-VERIFICATION.md` missing #3（重跑 Web+Desktop 隔离回归）。
Output: tenant-aware counter service + counter route tenant wiring + HTTP 级隔离回归测试。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-VERIFICATION.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-02-SUMMARY.md

<interfaces>
From `packages/core/domain/src/ports/mod.rs`:
```rs
pub struct TenantId(pub String);
```

From `packages/core/usecases/src/counter_service.rs`:
```rs
pub struct LibSqlCounterService<P: LibSqlPort> { ... }
pub const COUNTER_MIGRATION: &str = "...";
```

From `servers/api/src/routes/counter.rs` (current signatures):
```rs
async fn increment(State(state): State<AppState>) -> Json<serde_json::Value>;
async fn decrement(State(state): State<AppState>) -> Json<serde_json::Value>;
async fn reset(State(state): State<AppState>) -> Json<serde_json::Value>;
async fn get_value(State(state): State<AppState>) -> Json<serde_json::Value>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tenant-scoped counter operations in usecase layer</name>
  <files>packages/core/usecases/src/counter_service.rs</files>
  <read_first>
    - packages/core/usecases/src/counter_service.rs
    - packages/core/domain/src/ports/mod.rs
    - packages/core/domain/src/ports/lib_sql.rs
    - .planning/phases/10-multi-tenant-repeatable-verification-channel/10-VERIFICATION.md
    - .planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: tenant-A increment only changes tenant-A value; tenant-B value remains unchanged.
    - Test 2: tenant-A reset only resets tenant-A.
    - Test 3: repeated run with same tenant IDs starts from deterministic baseline after reset.
  </behavior>
  <action>In `counter_service.rs`, keep existing trait methods for non-tenant callers, and add explicit tenant-aware methods on `LibSqlCounterService` (e.g. `get_value_for_tenant`, `increment_for_tenant`, `decrement_for_tenant`, `reset_for_tenant`) that always bind by `tenant_id` string. Replace SQL that hardcodes `WHERE id = 1` with tenant-scoped SQL keyed by `tenant_id`. Update `COUNTER_MIGRATION` to define tenant-scoped storage (`tenant_id` key + `value` + `updated_at`) and keep migration idempotent (`CREATE TABLE IF NOT EXISTS`). Add unit tests in this file proving two tenant IDs do not share values. This task implements D-05/D-07 and closes the gap reason from VERIFICATION.md.</action>
  <acceptance_criteria>
    - `counter_service.rs` contains tenant-aware methods with `tenant_id` parameter.
    - SQL no longer depends on single global `id = 1` path for tenant-aware operations.
    - tests in `counter_service.rs` verify tenant-A and tenant-B values are isolated.
  </acceptance_criteria>
  <verify>
    <automated>rtk cargo test -p usecases counter_service -- --nocapture</automated>
  </verify>
  <done>Usecase layer exposes deterministic tenant-scoped counter read/write/reset behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire API counter handlers to TenantId and add HTTP isolation regression</name>
  <files>servers/api/src/routes/counter.rs, servers/api/tests/http_e2e_test.rs</files>
  <read_first>
    - servers/api/src/routes/counter.rs
    - servers/api/src/middleware/tenant.rs
    - servers/api/src/routes/mod.rs
    - servers/api/tests/http_e2e_test.rs
    - packages/core/usecases/src/counter_service.rs
    - .planning/phases/10-multi-tenant-repeatable-verification-channel/10-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: API call with token-A mutates only token-A's counter state.
    - Test 2: API call with token-B reads an unchanged value after token-A mutation.
    - Test 3: repeated HTTP sequence (A reset/B reset -> A increment -> A/B read) remains stable across two runs.
  </behavior>
  <action>Update all counter route handlers to read `TenantId` from request extensions (middleware output) and call the tenant-aware usecase methods from Task 1. Return 401/structured error when tenant context is missing instead of silently falling back to global state. In `http_e2e_test.rs`, add/extend API-level tests that initialize two users, run counter mutate/read with distinct bearer tokens, and assert no cross-tenant leakage across repeated runs. Keep assertions at behavior/API level per D-06 (no direct DB-level assertions).</action>
  <acceptance_criteria>
    - `counter.rs` handler signatures include tenant context extraction and pass tenant ID into service methods.
    - `http_e2e_test.rs` contains at least one cross-tenant counter isolation case with repeated-run assertions.
    - tests fail if tenant-A mutation affects tenant-B read path.
  </acceptance_criteria>
  <verify>
    <automated>rtk cargo test -p runtime_server --test http_e2e_test -- --nocapture</automated>
  </verify>
  <done>API counter behavior is tenant-scoped end-to-end and regression-tested at HTTP boundary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-run Web and Desktop tenant isolation regressions against tenant-scoped backend</name>
  <files>.planning/phases/10-multi-tenant-repeatable-verification-channel/10-04-SUMMARY.md</files>
  <read_first>
    - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
    - e2e-tests/wdio.conf.mjs
    - .planning/phases/10-multi-tenant-repeatable-verification-channel/10-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Web E2E 在 tenant-A 变更后，tenant-B 读取值保持不变。
    - Test 2: Desktop E2E 在两轮重复执行中保持 tenant-A/tenant-B 隔离与基线确定性。
    - Test 3: 端到端层面的结论与 Task 1/2 的后端 tenant-scoped 行为一致。
  </behavior>
  <action>在 Task 1/2 完成并通过后，重跑 Web Playwright 与 Desktop WDIO 的 tenant isolation 回归用例，验证 UI 层/客户端层对后端租户隔离的消费没有退化。将关键证据（命令、通过的测试名、若失败的最小复现）记录到 `10-04-SUMMARY.md`，用于关闭 `10-VERIFICATION.md` missing #3。</action>
  <acceptance_criteria>
    - Web 与 Desktop 都有 tenant isolation 回归重跑结果，且与后端 tenant-scoped 语义一致。
    - 回归测试在重复执行场景下稳定通过，不出现跨租户串值。
    - `10-04-SUMMARY.md` 记录可审计证据，足以支持 re-verification。
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd apps/client/web/app test:e2e -- --grep "tenant isolation"</automated>
    <automated>rtk bun run --cwd e2e-tests test:ci</automated>
  </verify>
  <done>Web+Desktop tenant isolation regressions are re-verified against real tenant-scoped backend behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT middleware → counter routes | Untrusted bearer token identity crosses into tenant data scope |
| Counter routes → libsql queries | Tenant context must be preserved to avoid cross-tenant data exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | E | `servers/api/src/routes/counter.rs` | mitigate | Require `TenantId` from middleware and reject missing tenant context instead of defaulting to shared row |
| T-10-11 | I | `packages/core/usecases/src/counter_service.rs` | mitigate | Tenant-scoped SQL key (`tenant_id`) for read/write/reset to prevent cross-tenant leakage |
| T-10-12 | R | `servers/api/tests/http_e2e_test.rs` | mitigate | Add deterministic repeated-run isolation test with token-A/token-B assertions and explicit failure messages |
</threat_model>

<verification>
- `rtk cargo test -p usecases counter_service -- --nocapture`
- `rtk cargo test -p runtime_server --test http_e2e_test -- --nocapture`
- `rtk bun run --cwd apps/client/web/app test:e2e -- --grep "tenant isolation"`
- `rtk bun run --cwd e2e-tests test:ci`
</verification>

<success_criteria>
- VERIFICATION gap truth #4 transitions from FAILED to VERIFIED.
- MTEN-02 has backend-enforced tenant isolation, not test-only assertions.
- `10-VERIFICATION.md` missing #3（Web+Desktop re-run evidence）被显式闭合并可审计。
</success_criteria>

<output>
After completion, create `.planning/phases/10-multi-tenant-repeatable-verification-channel/10-04-SUMMARY.md`
</output>
