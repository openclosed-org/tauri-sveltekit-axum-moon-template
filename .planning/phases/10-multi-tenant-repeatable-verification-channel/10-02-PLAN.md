---
phase: 10-multi-tenant-repeatable-verification-channel
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
  - e2e-tests/specs/tenant-isolation.e2e.mjs
  - e2e-tests/helpers/tenant.mjs
autonomous: true
requirements: [MTEN-02]
must_haves:
  truths:
    - "tenant-1 writes never change tenant-2 values"
    - "Isolation assertions remain true across repeated runs"
    - "Web and desktop suites both exercise the same fixed tenant pair"
  artifacts:
    - path: "apps/client/web/app/tests/e2e/tenant-isolation.test.ts"
      provides: "Playwright tenant isolation regression"
    - path: "e2e-tests/specs/tenant-isolation.e2e.mjs"
      provides: "WDIO desktop tenant isolation regression"
    - path: "e2e-tests/helpers/tenant.mjs"
      provides: "Desktop tenant reset and login helpers"
  key_links:
    - from: "apps/client/web/app/tests/e2e/tenant-isolation.test.ts"
      to: "apps/client/web/app/tests/fixtures/tenant.ts"
      via: "shared dual-tenant bootstrap"
      pattern: "initTenantPair"
    - from: "e2e-tests/specs/tenant-isolation.e2e.mjs"
      to: "e2e-tests/helpers/tenant.mjs"
      via: "desktop helper reuse"
      pattern: "resetTenantPair"
---

<objective>
把 tenant-1 / tenant-2 的隔离断言落到 Web + Desktop 双测试栈，并且让重复运行保持稳定。

Purpose: 证明租户隔离不是一次性通过，而是可重复回归。
Output: Web/desktop tenant isolation tests with explicit baseline reset.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-DISCUSSION-LOG.md

<interfaces>
From `apps/client/web/app/tests/e2e/tenant-isolation.test.ts`:
```ts
test.describe('Tenant Isolation (E2E)', () => { ... });
```

From `e2e-tests/scripts/run-desktop-e2e.mjs`:
```js
spawnSync('wdio', ['run', 'wdio.conf.mjs'], { cwd: e2eRoot, ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen Playwright isolation coverage with repeatable reset</name>
  <files>apps/client/web/app/tests/e2e/tenant-isolation.test.ts</files>
  <read_first>
    - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
    - apps/client/web/app/tests/fixtures/tenant.ts
    - apps/client/web/app/tests/fixtures/auth.ts
    - servers/api/src/routes/counter.rs
    - servers/api/src/routes/tenant.rs
  </read_first>
  <behavior>
    - Test 1: tenant-1 counter mutation does not alter tenant-2 value
    - Test 2: repeated runs start from the same known counter baseline
    - Test 3: failure output makes the affected tenant label obvious
  </behavior>
  <action>Rewrite the existing tenant isolation E2E into an explicit write-read isolation flow: bootstrap both tenants, reset them to the same counter value, mutate tenant-1, verify tenant-2 remains unchanged, then rerun the same flow to prove determinism. Keep assertions behavior-level, not storage-level, and keep the tenant label visible in the test naming and failure messages.</action>
  <acceptance_criteria>
    - `tenant-isolation.test.ts` contains a tenant-1/tenant-2 write-read assertion.
    - test setup explicitly resets both tenants before the assertion.
    - repeated execution uses the same seed values and passes the same assertion set.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/client/web/app test:e2e --grep "tenant isolation"</automated>
  </verify>
  <done>Web E2E proves tenant-scoped counter isolation with repeatable baseline reset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add WDIO desktop tenant isolation regression</name>
  <files>e2e-tests/specs/tenant-isolation.e2e.mjs, e2e-tests/helpers/tenant.mjs</files>
  <read_first>
    - e2e-tests/specs/counter.e2e.mjs
    - e2e-tests/specs/agent.e2e.mjs
    - e2e-tests/scripts/run-desktop-e2e.mjs
    - e2e-tests/wdio.conf.mjs
    - .planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: desktop path exercises the same fixed tenant pair as Web
    - Test 2: desktop isolation test reports a clear failure when the value leaks
    - Test 3: desktop helper avoids manual environment patching
  </behavior>
  <action>Create a dedicated WDIO spec that logs in with tenant-A / tenant-B, resets both counters, mutates tenant-A, and verifies tenant-B remains unchanged. Keep the helper logic in `e2e-tests/helpers/tenant.mjs` so the Desktop suite can share the same repeatable identity model without importing web test utilities.</action>
  <acceptance_criteria>
    - desktop spec file exists and is picked up by the current WDIO glob.
    - desktop spec contains explicit tenant-1/tenant-2 isolation assertions.
    - helper file provides repeatable bootstrap/reset entrypoints.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd e2e-tests test:ci</automated>
  </verify>
  <done>Desktop E2E covers the same tenant isolation regression as Web.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → multi-tenant state | Incorrect setup can create false positives or false negatives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | R | isolation regression | mitigate | Reset both tenants to the same baseline before each run |
| T-10-05 | I | failure reporting | mitigate | Keep tenant label and value diff visible in assertions |
| T-10-06 | D | desktop repeatability | accept | Local test-only regression with bounded login/reset flow |
</threat_model>

<verification>
- `bun run --cwd apps/client/web/app test:e2e --grep "tenant isolation"`
- `bun run --cwd e2e-tests test:ci`
</verification>

<success_criteria>
- MTEN-02 is verified by both Web and Desktop regressions, and repeated runs stay stable.
</success_criteria>

<output>
After completion, create `.planning/phases/10-multi-tenant-repeatable-verification-channel/10-02-SUMMARY.md`
</output>
