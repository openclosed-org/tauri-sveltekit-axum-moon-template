---
phase: 10-multi-tenant-repeatable-verification-channel
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - .github/workflows/e2e-tests.yml
  - apps/client/web/app/playwright.config.ts
  - e2e-tests/wdio.conf.mjs
  - e2e-tests/scripts/run-desktop-e2e.mjs
autonomous: true
requirements: [MTEN-03]
must_haves:
  truths:
    - "CI uploads enough evidence to diagnose tenant isolation failures"
    - "Artifacts are job-scoped and retained for 7 days"
    - "Playwright and WDIO both preserve the tenant mapping evidence"
  artifacts:
    - path: ".github/workflows/e2e-tests.yml"
      provides: "Job-scoped artifact uploads and retention"
    - path: "apps/client/web/app/playwright.config.ts"
      provides: "Web E2E evidence settings"
    - path: "e2e-tests/wdio.conf.mjs"
      provides: "Desktop JUnit and logs output"
  key_links:
    - from: ".github/workflows/e2e-tests.yml"
      to: "apps/client/web/app/playwright.config.ts"
      via: "playwright trace/video/screenshot artifacts"
      pattern: "upload-artifact"
    - from: ".github/workflows/e2e-tests.yml"
      to: "e2e-tests/wdio.conf.mjs"
      via: "WDIO JUnit and desktop evidence"
      pattern: "wdio-junit"
---

<objective>
把多租户测试的失败证据固化为 CI 可下载的最小诊断包，并确保证据按 job 独立归档。

Purpose: 让 MTEN 失败能直接定位到 tenant mapping、trace、video、screenshot 和 JUnit。
Output: job-level artifact uploads with 7-day retention.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-DISCUSSION-LOG.md

<interfaces>
From `.github/workflows/e2e-tests.yml`:
```yaml
- name: Upload Playwright Report
  uses: actions/upload-artifact@v4
```

From `apps/client/web/app/playwright.config.ts`:
```ts
trace: 'on-first-retry', screenshot: 'only-on-failure', video: 'retain-on-failure'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand Web E2E artifact output for tenant diagnostics</name>
  <files>.github/workflows/e2e-tests.yml, apps/client/web/app/playwright.config.ts</files>
  <read_first>
    - .github/workflows/e2e-tests.yml
    - apps/client/web/app/playwright.config.ts
    - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
    - .planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Web job uploads trace/video/screenshot for failures
    - Test 2: tenant mapping evidence is available as a downloadable artifact
    - Test 3: artifact retention is capped at 7 days
  </behavior>
  <action>Update the Web E2E job to publish a job-scoped artifact bundle that includes the Playwright report and tenant-mapping evidence. Keep the existing failure-only trace/video/screenshot policy, and make the job name explicit enough that the artifact set can be traced back to a single runner. Set retention to 7 days to match the phase decision.</action>
  <acceptance_criteria>
    - workflow has a job-scoped Playwright artifact upload.
    - uploaded bundle contains failure diagnostics and tenant mapping evidence.
    - retention is configured to 7 days.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "upload-artifact\|retention-days\|playwright-report" .github/workflows/e2e-tests.yml</automated>
    <manual>在 CI 触发一次 `e2e-tests.yml`（包含 web 与 desktop jobs），并在 run 页面确认 web job 产出 job-scoped Playwright artifact，artifact 名称含 job 维度标识，`retention-days` 显示为 7。</manual>
  </verify>
  <done>Web CI failures produce a minimal, job-level diagnostic bundle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add WDIO evidence uploads for desktop tenant failures</name>
  <files>e2e-tests/wdio.conf.mjs, e2e-tests/scripts/run-desktop-e2e.mjs, .github/workflows/e2e-tests.yml</files>
  <read_first>
    - e2e-tests/wdio.conf.mjs
    - e2e-tests/scripts/run-desktop-e2e.mjs
    - .github/workflows/e2e-tests.yml
    - e2e-tests/specs/tenant-isolation.e2e.mjs
  </read_first>
  <behavior>
    - Test 1: desktop JUnit output remains enabled
    - Test 2: desktop logs and evidence files are archived per job
    - Test 3: uploads occur even when tests fail
  </behavior>
  <action>Keep WDIO JUnit generation and extend the desktop E2E job to upload the job-local evidence bundle in `if: always()` steps. Include the WDIO XML, any tenant mapping file, and any extra logs produced by the desktop helper so failures can be diagnosed without rerunning locally.</action>
  <acceptance_criteria>
    - WDIO JUnit output remains enabled in the config.
    - workflow uploads desktop evidence in `if: always()`.
    - evidence upload is job-scoped, not PR-aggregated.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "wdio-junit\|if: always()\|upload-artifact" e2e-tests/wdio.conf.mjs .github/workflows/e2e-tests.yml</automated>
    <manual>同一 CI run 中故意制造一例 desktop tenant 断言失败，确认 desktop job 在失败后仍上传 evidence artifact，且可下载内容包含 WDIO JUnit XML、desktop 日志与 tenant 映射证据文件。</manual>
  </verify>
  <done>Desktop CI failures ship enough evidence to diagnose tenant issues locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job → artifact store | Evidence must be scoped per job and retained predictably |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | I | artifact contents | mitigate | Include only minimal diagnostic package, not secrets |
| T-10-08 | D | CI evidence retention | mitigate | Retain artifacts for 7 days and scope them by job |
| T-10-09 | R | failure traceability | mitigate | Upload report files even on failure using `if: always()` |
</threat_model>

<verification>
- `grep -n "retention-days\|upload-artifact\|if: always()" .github/workflows/e2e-tests.yml`
- `grep -n "trace\|video\|screenshot" apps/client/web/app/playwright.config.ts`
- `grep -n "wdio-junit" e2e-tests/wdio.conf.mjs`
</verification>

<success_criteria>
- MTEN-03 failures can be diagnosed from CI artifacts alone.
</success_criteria>

<output>
After completion, create `.planning/phases/10-multi-tenant-repeatable-verification-channel/10-03-SUMMARY.md`
</output>
