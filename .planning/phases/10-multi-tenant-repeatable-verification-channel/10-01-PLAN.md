---
phase: 10-multi-tenant-repeatable-verification-channel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/web/app/tests/fixtures/tenant.ts
  - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
  - apps/client/web/app/tests/e2e/counter.test.ts
autonomous: true
requirements: [MTEN-01]
must_haves:
  truths:
    - "Tester can switch between at least two tenants without manual environment patching"
    - "Tenant mapping is fixed and repeatable across runs"
    - "Tenant init failure stops the test immediately instead of falling back silently"
  artifacts:
    - path: "apps/client/web/app/tests/fixtures/tenant.ts"
      provides: "Fixed tenant mapping and reset helper"
    - path: "apps/client/web/app/tests/e2e/tenant-isolation.test.ts"
      provides: "Repeatable tenant harness entrypoint"
    - path: "apps/client/web/app/tests/e2e/counter.test.ts"
      provides: "Counter flow reuse of tenant harness"
  key_links:
    - from: "apps/client/web/app/tests/fixtures/tenant.ts"
      to: "servers/api/src/routes/tenant.rs"
      via: "POST /api/tenant/init"
      pattern: "api/tenant/init"
    - from: "apps/client/web/app/tests/e2e/tenant-isolation.test.ts"
      to: "apps/client/web/app/tests/fixtures/auth.ts"
      via: "mock OAuth login and tenant bootstrap"
      pattern: "triggerMockOAuth"
---

<objective>
建立固定双租户测试 harness，使 Web E2E 能在不改环境变量、不打补丁的前提下稳定切换 tenant-A / tenant-B。

Purpose: 为后续隔离断言和 CI 诊断提供可重复的身份/初始化入口。
Output: 双租户 fixture + 可复用初始化/重置 helper。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md
@.planning/phases/10-multi-tenant-repeatable-verification-channel/10-DISCUSSION-LOG.md

<interfaces>
From `servers/api/src/routes/tenant.rs`:
```rs
pub async fn init_tenant(State(state): State<AppState>, Json(body): Json<InitTenantRequest>) -> AppResult<Json<Value>>;
```

From `apps/client/web/app/tests/fixtures/auth.ts`:
```ts
export async function triggerMockOAuth(page: Page, mockCode?: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a fixed dual-tenant fixture and reset helper</name>
  <files>apps/client/web/app/tests/fixtures/tenant.ts</files>
  <read_first>
    - apps/client/web/app/tests/fixtures/auth.ts
    - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
    - servers/api/src/routes/tenant.rs
    - .planning/phases/10-multi-tenant-repeatable-verification-channel/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: harness exposes exactly two stable tenant identities (tenant-A and tenant-B)
    - Test 2: helper resets both tenants to a known counter baseline before each case
    - Test 3: init failure aborts the current test with a visible error message
  </behavior>
  <action>Create `tests/fixtures/tenant.ts` with fixed tenant mapping constants, `initTenantPair()`, and `resetTenantPairCounter()` helpers. Reuse the existing mock OAuth fixture for authentication, but keep tenant identity deterministic and file-backed so repeated runs always target the same two users. On any init failure, throw immediately and include the tenant label in the error text.</action>
  <acceptance_criteria>
    - `tenant.ts` exports fixed tenant labels and init/reset helpers.
    - helper code calls the tenant init API rather than mutating state directly.
    - failure path includes the tenant label and does not continue with a partial setup.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/client/web/app test:e2e --grep "tenant"</automated>
  </verify>
  <done>Web tests can reliably bootstrap two fixed tenants without manual environment editing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → tenant init API | Untrusted test input must not create nondeterministic tenant mapping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | tenant fixture mapping | mitigate | Keep mapping fixed and file-backed, never randomize per run |
| T-10-02 | R | init/reset helper | mitigate | Fail fast with tenant label in error message |
| T-10-03 | D | repeatable bootstrap | accept | Local test-only flow, bounded by existing auth and tenant init APIs |
</threat_model>

<verification>
- `bun run --cwd apps/client/web/app test:e2e --grep "tenant"`
- `bun run --cwd apps/client/web/app test:unit -- tests/component` if shared fixture helpers are reused by component tests
</verification>

<success_criteria>
- MTEN-01 is covered by a deterministic dual-tenant harness.
</success_criteria>

<output>
After completion, create `.planning/phases/10-multi-tenant-repeatable-verification-channel/10-01-SUMMARY.md`
</output>
