---
phase: 03-application-pages
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-ui/src/routes/(auth)/+layout.svelte
  - apps/desktop-ui/src/routes/(app)/+layout.svelte
  - apps/desktop-ui/src/routes/(auth)/login/+page.svelte
  - apps/desktop-ui/src/routes/+page.svelte
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "User can see login page at /login"
    - "User can see counter page at /counter with working increment/decrement"
    - "User can see admin dashboard at /admin with sidebar navigation"
    - "Navigation works between pages without full reload"
    - "Layout adapts: mobile shows bottom tabs, desktop shows collapsible sidebar"
  artifacts:
    - path: "apps/desktop-ui/src/routes/(auth)/+layout.svelte"
      provides: "Auth layout without navigation chrome"
      min_lines: 15
    - path: "apps/desktop-ui/src/routes/(app)/+layout.svelte"
      provides: "App layout with responsive navigation"
      min_lines: 30
    - path: "apps/desktop-ui/src/routes/(auth)/login/+page.svelte"
      provides: "Login page with branded layout"
      min_lines: 20
    - path: "apps/desktop-ui/src/routes/+page.svelte"
      provides: "Redirect to login or counter based on auth state (placeholder)"
  key_links:
    - from: "apps/desktop-ui/src/routes/(app)/+layout.svelte"
      to: "$page.url.pathname"
      via: "SvelteKit $page store"
      pattern: "\\$page"
    - from: "(app)/+layout.svelte"
      to: "$lib/components"
      via: "Import Button, Switch, Icons"
      pattern: "import.*\\$lib/components"
---

<objective>
Create routing infrastructure: route groups (auth, app), shared layouts with responsive navigation, and login page redirect. This establishes the navigation shell that all page plans depend on.

Purpose: Foundation for three core pages - login (unauthenticated), counter and admin (authenticated app pages)
Output: Route group layouts, navigation components, login page redirect
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-application-pages/03-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Design decisions from context (MANDATORY)
- D-01: SvelteKit route groups: (auth) for login, (app) for counter + admin
- D-02: (auth)/login — standalone page, no sidebar, no tab bar
- D-03: (app)/+layout.svelte — shared layout wrapping counter and admin pages
- D-04: URL paths: /login, /counter, /admin
- D-05: Mobile (<800px): bottom tab bar with 3 tabs (Counter, Admin, Settings)
- D-06: Desktop (>=800px): left collapsible sidebar
- D-07: md (800px): sidebar defaults to icon-only collapsed (~64px)
- D-08: lg (1280px): sidebar defaults to icon+label expanded (~240px)
- D-09: User can manually toggle sidebar expand/collapse
- D-10: Sidebar content: app logo+name (top), nav links with icons (middle), settings/theme toggle (bottom)
- D-11: Login page has no navigation chrome (standalone (auth) layout)

# Phase 2 dependencies (MUST READ)
- apps/desktop-ui/src/lib/components/index.ts — Available components: Button, Input, Card, Badge, Switch, Toast, etc.
- apps/desktop-ui/src/app.css — Theme tokens: primary-*, gray-*, breakpoints: sm(375px), md(800px), lg(1280px)
- apps/desktop-ui/src/lib/stores/theme.ts — getTheme/setTheme/toggleTheme for dark mode

# Existing code to extend
- apps/desktop-ui/src/routes/+layout.svelte — Root layout (imports app.css, renders children)
- apps/desktop-ui/src/routes/+layout.ts — SPA mode (ssr=false, prerender=false)
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From apps/desktop-ui/src/lib/components/index.ts:
```typescript
export { default as Button } from "./ui/Button.svelte";
export { default as Input } from "./ui/Input.svelte";
export { default as Card } from "./ui/Card.svelte";
export { default as Switch } from "./ui/Switch.svelte";
// Available: Badge, Toast, Dialog, Select, Dropdown
```

From apps/desktop-ui/src/app.css (breakpoints):
```css
--breakpoint-sm: 375px;  /* mobile */
--breakpoint-md: 800px;  /* tablet/windowed - sidebar threshold */
--breakpoint-lg: 1280px; /* desktop - expanded sidebar */
```

From apps/desktop-ui/src/lib/stores/theme.ts:
```typescript
// getTheme(): 'light' | 'dark'
// setTheme(theme: 'light' | 'dark')
// toggleTheme(): void
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create (auth) route group with login-only layout</name>
  <files>
    - apps/desktop-ui/src/routes/(auth)/+layout.svelte
  </files>
  <read_first>
    - apps/desktop-ui/src/routes/+layout.svelte
    - apps/desktop-ui/src/app.css
  </read_first>
  <action>
Create apps/desktop-ui/src/routes/(auth)/+layout.svelte:
- Import ../app.css at top level (so auth pages have theme)
- Render children() with NO navigation chrome (no sidebar, no tab bar)
- Use full viewport height: min-h-screen, bg-[var(--color-bg)]
- Center content vertically using flexbox

This layout wraps only login page. No navigation elements.
  </action>
  <acceptance_criteria>
- File exists at exact path: apps/desktop-ui/src/routes/(auth)/+layout.svelte
- Contains import "../app.css" or "../../app.css" (from subdirectory)
- Uses $props() to get children
- Has NO sidebar, NO bottom tabs, NO navigation
- Full viewport layout: min-h-screen, flex, center content
  </acceptance_criteria>
  <verify>
    <automated>grep -q "app.css" "apps/desktop-ui/src/routes/(auth)/+layout.svelte" && grep -q "min-h-screen" "apps/desktop-ui/src/routes/(auth)/+layout.svelte" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Auth-only layout created without navigation chrome</done>
</task>

<task type="auto">
  <name>Task 2: Create (app) route group with responsive navigation layout</name>
  <files>
    - apps/desktop-ui/src/routes/(app)/+layout.svelte
  </files>
  <read_first>
    - apps/desktop-ui/src/routes/+layout.svelte
    - apps/desktop-ui/src/app.css
    - apps/desktop-ui/src/lib/components/index.ts
    - apps/desktop-ui/src/lib/stores/theme.ts (if exists)
  </read_first>
  <action>
Create apps/desktop-ui/src/routes/(app)/+layout.svelte:
- Import ../../app.css for theme
- Get children via $props()

Responsive navigation:
1. Mobile (< md/800px): Bottom tab bar
   - Fixed position bottom, h-14, bg-[var(--color-bg-elevated)]
   - 3 tabs: Counter (/counter), Admin (/admin), Settings (placeholder)
   - Use @lucide/svelte icons: Calculator, Gauge, Settings
   - Active tab: text-[var(--color-primary-600)], inactive: text-[var(--color-text-muted)]

2. Desktop (>= md/800px): Left collapsible sidebar
   - w-64 expanded, w-16 collapsed (icon-only)
   - Top: app logo + "Tauri App" text (placeholder for now)
   - Middle: nav links with icons
   - Bottom: theme toggle (Switch component from $lib/components)
   - User can click toggle to expand/collapse sidebar

State: use $state for sidebarExpanded (boolean), activeTab (string)

Use CSS transitions for expand/collapse animation (150ms ease).
  </action>
  <acceptance_criteria>
- File exists at exact path: apps/desktop-ui/src/routes/(app)/+layout.svelte
- Mobile: shows bottom tab bar with 3 tabs (Counter, Admin, Settings)
- Desktop: shows collapsible left sidebar (~64px collapsed, ~240px expanded)
- Uses $state for sidebarExpanded (Svelte 5 rune)
- Imports components from $lib/components (Button, Switch)
- Theme toggle present using Switch component
- Uses @lucide/svelte icons
  </acceptance_criteria>
  <verify>
    <automated>grep -q "md:" "apps/desktop-ui/src/routes/(app)/+layout.svelte" && grep -q "sidebarExpanded" "apps/desktop-ui/src/routes/(app)/+layout.svelte" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>App layout with responsive navigation created</done>
</task>

<task type="auto">
  <name>Task 3: Create login route and redirect root</name>
  <files>
    - apps/desktop-ui/src/routes/(auth)/login/+page.svelte
    - apps/desktop-ui/src/routes/+page.svelte
  </files>
  <read_first>
    - apps/desktop-ui/src/routes/(auth)/+layout.svelte (created in Task 1)
    - apps/desktop-ui/src/lib/components/index.ts
  </read_first>
  <action>
Part A: Create apps/desktop-ui/src/routes/(auth)/login/+page.svelte
- This page renders WITHIN the (auth) layout (no nav chrome)
- Layout: D-12 through D-16 from context
- Top: App logo + name + "欢迎使用" subtitle
- Center: Google sign-in Button (primary variant, from $lib/components)
- Below button: Email input field (disabled, placeholder="Email coming soon")
- Mobile: full-screen, no padding constraints, fills viewport
- Desktop: centered card layout (~400px max-width, vertically centered)

Part B: Update apps/desktop-ui/src/routes/+page.svelte
- Redirect to /login (for now, until auth is implemented in Phase 6)
- Use SvelteKit's goto() or just render link: <a href="/login">Go to Login</a>
- Simple placeholder for Phase 3
  </action>
  <acceptance_criteria>
- (auth)/login/+page.svelte exists with app logo/branding
- Contains Button with Google sign-in text
- Contains Input (disabled) for email placeholder
- Desktop: centered card with max-width ~400px
- Mobile: full viewport padding
- Root page redirects to /login or shows link
  </acceptance_criteria>
  <verify>
    <automated>ls "apps/desktop-ui/src/routes/(auth)/login/+page.svelte" && grep -q "login" "apps/desktop-ui/src/routes/+page.svelte"</automated>
  </verify>
  <done>Login page created at /login with branded layout</done>
</task>

</tasks>

<verification>
- [ ] (auth)/+layout.svelte renders without navigation
- [ ] (app)/+layout.svelte shows bottom tabs on mobile, sidebar on desktop
- [ ] Sidebar collapses/expands with toggle
- [ ] Login page at /login shows branded layout with Google button
- [ ] Root page redirects to /login
- [ ] All pages use $state (Svelte 5 runes), not $store
</verification>

<success_criteria>
User can navigate to /login and see branded login page without navigation chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/03-application-pages/03-01-SUMMARY.md`
</output>