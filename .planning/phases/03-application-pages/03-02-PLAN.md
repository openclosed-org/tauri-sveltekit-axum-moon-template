---
phase: 03-application-pages
plan: '02'
type: execute
wave: 2
depends_on:
  - '03-01'
files_modified:
  - apps/desktop-ui/src/routes/(app)/counter/+page.svelte
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Counter displays at /counter with large number"
    - "Increment (+) increases counter by 1"
    - "Decrement (-) decreases counter by 1 (can go negative)"
    - "Reset button sets counter back to 0"
    - "Counter uses Svelte 5 $state rune"
  artifacts:
    - path: "apps/desktop-ui/src/routes/(app)/counter/+page.svelte"
      provides: "Interactive counter with increment/decrement/reset"
      min_lines: 40
  key_links:
    - from: "(app)/counter/+page.svelte"
      to: "(app)/+layout.svelte"
      via: "Renders within app layout with navigation"
---

<objective>
Create Counter page at /counter with reactive $state rune. Large centered number display with increment (+), decrement (-), and reset buttons.

Purpose: Showcase Svelte 5 runes ($state) for boilerplate users - the counter is a simple but complete reactive component demonstrating runes in action.
Output: Counter page with working increment/decrement/reset
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-application-pages/03-CONTEXT.md §Counter Page

# From context (design decisions):
- D-17: Large centered number display (showcases Svelte 5 $state rune)
- D-18: Increment (+) and decrement (-) buttons flanking the number
- D-19: Reset button below the counter
- D-20: Counter can go negative (no minimum bound)
- D-21: Always centered layout, larger touch targets on mobile (h-12+ buttons)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Counter page with $state rune</name>
  <files>
    - apps/desktop-ui/src/routes/(app)/counter/+page.svelte
  </files>
  <read_first>
    - apps/desktop-ui/src/routes/(app)/+layout.svelte (verify layout structure)
    - apps/desktop-ui/src/lib/components/index.ts (for Button component)
  </read_first>
  <action>
Create apps/desktop-ui/src/routes/(app)/counter/+page.svelte:

1. Script section:
   - Import Button from $lib/components
   - Use $state rune for counter: let count = $state(0)
   - Create increment function: () => count++
   - Create decrement function: () => count--
   - Create reset function: () => count = 0

2. Layout:
   - Full viewport centered: flex flex-col items-center justify-center min-h-screen
   - Gap between elements: gap-6

3. Counter display:
   - Large number: text-8xl or text-9xl
   - Font: font-mono (monospace for numerical stability)
   - Color: text-[var(--color-text)]
   - Padding: py-8

4. Buttons:
   - flanking the number in a row: flex flex-row gap-4
   - Increment (+): Button with icon (Plus from @lucide/svelte), variant primary
   - Decrement (-): Button with icon (Minus from @lucide/svelte), variant secondary
   - Reset: Button with "Reset" text, variant ghost/destructive
   - Mobile touch targets: h-12 (48px) minimum, w-12 for icon buttons
   - Use Button size "lg" for larger touch targets

5. Styling:
   - Background: var(--color-bg)
   - Use theme colors throughout: primary-*, gray-*
   - Add subtle hover states on buttons

Svelte 5 runes are REQUIRED - use $state, NOT $store or useState.
  </action>
  <acceptance_criteria>
- File exists at: apps/desktop-ui/src/routes/(app)/counter/+page.svelte
- Uses $state(0) for counter (Svelte 5 rune, NOT $store)
- Increment button increases count by 1
- Decrement button decreases count by 1 (can go negative)
- Reset button sets count back to 0
- Counter number is large (text-8xl or larger), centered, monospace
- Mobile: buttons have h-12+ touch targets
- Uses Button component from $lib/components
- Uses Plus/Minus icons from @lucide/svelte
  </acceptance_criteria>
  <verify>
    <automated>grep -q "\\$state" "apps/desktop-ui/src/routes/(app)/counter/+page.svelte" && grep -q "text-8xl" "apps/desktop-ui/src/routes/(app)/counter/+page.svelte" && grep -q "h-12" "apps/desktop-ui/src/routes/(app)/counter/+page.svelte" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Counter page functional with $state rune, all three buttons work</done>
</task>

</tasks>

<verification>
- [ ] /counter renders with large centered number
- [ ] Clicking + increases counter
- [ ] Clicking - decreases counter (can go negative)
- [ ] Clicking Reset returns to 0
- [ ] Uses $state rune (verifiable in code)
- [ ] Mobile: touch targets are accessible size
</verification>

<success_criteria>
User can interact with counter: increment, decrement, reset. Counter uses Svelte 5 $state rune as required by the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/03-application-pages/03-02-SUMMARY.md`
</output>