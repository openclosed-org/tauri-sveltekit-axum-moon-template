---
phase: 10.1-docs-storage-compile-requirements-md-turso-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/api/src/config.rs
  - servers/api/src/state.rs
  - servers/api/src/main.rs
  - servers/api/src/state.rs
autonomous: true
requirements: [MTEN-01]
must_haves:
  truths:
    - "服务端默认启动使用 Turso 文件库而非 memory"
    - "数据库路径缺失/连接失败/迁移失败时启动立即失败"
    - "启动日志可看到 provider、绝对路径与来源（default/env）"
  artifacts:
    - path: "servers/api/src/config.rs"
      provides: "Turso-only 默认 provider + 文件路径配置"
    - path: "servers/api/src/state.rs"
      provides: "Turso 初始化 + fail-fast 启动契约"
    - path: "servers/api/src/main.rs"
      provides: "配置化启动入口，不再固定 new_dev memory 路径"
  key_links:
    - from: "servers/api/src/main.rs"
      to: "servers/api/src/state.rs"
      via: "AppState::new_with_config(config)"
      pattern: "new_with_config"
    - from: "servers/api/src/state.rs"
      to: "storage_turso::EmbeddedTurso"
      via: "Builder::new_local(file_path)"
      pattern: "EmbeddedTurso::new"
---

<objective>
收敛 runtime_server 默认启动链路为 Turso 文件持久化并建立 fail-fast 契约。

Purpose: 落实 D-01/D-02/D-03/D-09/D-10/D-11/D-12，消除默认 memory 路径与静默降级风险。
Output: 服务端配置、状态初始化与启动入口完成 Turso-only 默认收敛并可观测。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/STORAGE_COMPILE_REQUIREMENTS.md
@.planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-CONTEXT.md
@servers/api/src/config.rs
@servers/api/src/state.rs
@servers/api/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固化 runtime_server Turso-only 默认配置与路径来源契约</name>
  <read_first>servers/api/src/config.rs, docs/STORAGE_COMPILE_REQUIREMENTS.md, .planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-CONTEXT.md</read_first>
  <files>servers/api/src/config.rs</files>
  <behavior>
    - Test 1: Config::default().database.provider 等于 Turso。
    - Test 2: 默认 database.url 非 memory/:memory:，且是固定文件路径。
    - Test 3: 支持环境变量覆盖数据库路径并可识别来源（default/env）。
  </behavior>
  <action>在 `servers/api/src/config.rs` 中将默认 provider 改为 Turso（D-01），默认数据库 URL 改为 `servers/api/.data/runtime_server.db` 等文件路径（D-10）；新增路径来源字段或辅助解析函数（例如 `resolved_db_path()` 返回绝对路径与来源 default/env，D-12）；明确拒绝默认 memory/:memory:（D-02），并为该规则补单测。禁止保留 SurrealDB 作为默认值。</action>
  <acceptance_criteria>- `CloudDbProvider` 默认值为 `Turso`。
- `Config::default()` 结果中不包含 `memory` 或 `:memory:`。
- 存在可测试的路径来源解析逻辑（包含 `default` 与 `env`）。
- `cargo test -p runtime_server --lib config::tests` 通过。</acceptance_criteria>
  <verify>
    <automated>cargo test -p runtime_server --lib config::tests -- --nocapture</automated>
  </verify>
  <done>服务端默认配置已收敛为 Turso 文件库且可区分路径来源，默认运行不再落入 memory。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 切换服务端启动入口为配置化 Turso 初始化并 fail-fast</name>
  <read_first>servers/api/src/state.rs, servers/api/src/main.rs, servers/api/src/config.rs, packages/adapters/storage/turso/src/embedded.rs</read_first>
  <files>servers/api/src/state.rs, servers/api/src/main.rs</files>
  <behavior>
    - Test 1: 启动时创建不存在的父目录并初始化 Turso 文件库成功。
    - Test 2: 路径不可写/连接失败/迁移失败时，启动返回错误并中止。
    - Test 3: 启动日志包含 provider + 绝对路径 + 来源，不输出 auth token。
  </behavior>
  <action>在 `state.rs` 中重构 `new_with_config` 为 Turso 文件库初始化主路径：解析绝对路径、父目录自动创建（D-11）、执行 tenant/counter/agent migrations，任一失败直接返回错误（D-03）；移除/隔离默认 `new_dev` memory 路径使其仅测试可用（D-02）。在 `main.rs` 改为 `Config::from_env().unwrap_or_default()` + `AppState::new_with_config(config)`，并打印 provider、最终绝对路径、来源（default/env，D-12），严格避免日志输出 token/secret。</action>
  <acceptance_criteria>- `main.rs` 不再调用 `AppState::new_dev()` 作为默认启动路径。
- `state.rs` 启动链路包含父目录自动创建与迁移执行。
- `state.rs`/`main.rs` 中存在 provider/path/source 日志字段。
- 日志中无 `auth_token`/`secret` 明文字段。
- `cargo test -p runtime_server --lib state::tests` 与 `cargo test -p runtime_server --test integration_test` 可通过。</acceptance_criteria>
  <verify>
    <automated>cargo test -p runtime_server --lib && cargo test -p runtime_server --test integration_test</automated>
  </verify>
  <done>runtime_server 默认启动已完全走 Turso 文件库并具备 fail-fast + 可观测路径，不再默认依赖 memory。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process→filesystem | 启动阶段写入/创建 DB 路径，涉及路径注入与权限风险 |
| env→config | 环境变量覆盖 DB 路径，属于不可信输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-01 | T | servers/api/src/config.rs | mitigate | 对 env 路径做 canonicalize 与 memory 路径拒绝，限制默认回退策略 |
| T-10.1-02 | I | servers/api/src/main.rs logging | mitigate | 日志白名单仅输出 provider/path/source，显式不记录 token/secret |
| T-10.1-03 | D | servers/api/src/state.rs startup | mitigate | 连接/迁移失败立即返回 error，阻止带坏状态继续提供服务 |
</threat_model>

<verification>
- `cargo test -p runtime_server --lib`
- `cargo test -p runtime_server --test integration_test`
</verification>

<success_criteria>
- 默认服务端启动路径不再使用 `memory` / `:memory:`。
- Turso 文件路径在启动日志可观测（provider/path/source）。
- 初始化失败触发 fail-fast，且无降级到内存库行为。
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-01-SUMMARY.md`
</output>
