---
phase: 10.1-docs-storage-compile-requirements-md-turso-only
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/native/src-tauri/src/lib.rs
  - apps/client/native/src-tauri/Cargo.toml
  - packages/adapters/hosts/tauri/src/commands/counter.rs
autonomous: true
requirements: [MTEN-01]
must_haves:
  truths:
    - "桌面端默认将业务数据写入 Turso 文件库并在重启后可复用"
    - "桌面端默认路径不是 :memory:，且支持 env 覆盖"
    - "启动日志可确认 provider/path/source 且不泄露敏感信息"
  artifacts:
    - path: "apps/client/native/src-tauri/src/lib.rs"
      provides: "native-tauri Turso 文件库初始化与路径观测"
    - path: "apps/client/native/src-tauri/Cargo.toml"
      provides: "桌面默认编译链路对 Turso-only 的依赖声明"
  key_links:
    - from: "apps/client/native/src-tauri/src/lib.rs"
      to: "storage_turso::EmbeddedTurso"
      via: "EmbeddedTurso::new(file_path)"
      pattern: "EmbeddedTurso::new"
---

<objective>
收敛 native-tauri 默认数据落点到 Turso 文件库并建立路径可观测契约。

Purpose: 落实 D-01/D-02/D-03/D-09/D-11/D-12，保证桌面端默认运行不依赖内存库。
Output: 桌面端数据库初始化、路径解析、日志输出与失败语义对齐阶段决策。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/STORAGE_COMPILE_REQUIREMENTS.md
@.planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-CONTEXT.md
@apps/client/native/src-tauri/src/lib.rs
@apps/client/native/src-tauri/Cargo.toml
@packages/adapters/storage/turso/src/embedded.rs
@packages/adapters/hosts/tauri/src/commands/counter.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: native-tauri 默认 DB 路径改为文件库并支持来源标识</name>
  <read_first>apps/client/native/src-tauri/src/lib.rs, docs/STORAGE_COMPILE_REQUIREMENTS.md, .planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-CONTEXT.md</read_first>
  <files>apps/client/native/src-tauri/src/lib.rs</files>
  <behavior>
    - Test 1: 默认数据库路径为固定文件路径（非 :memory:）。
    - Test 2: 支持环境变量覆盖并可识别来源 default/env。
    - Test 3: 父目录不存在时自动创建，失败时启动中止。
  </behavior>
  <action>在 `native/src-tauri/src/lib.rs` 替换 `EmbeddedTurso::new(":memory:")` 为文件路径初始化（D-01/D-02）；默认路径使用平台 app data 目录下固定文件名（D-09），并允许通过环境变量覆盖；对父目录执行自动创建（D-11），目录创建/连接/迁移失败统一 `expect`/error fail-fast（D-03）；启动日志打印 provider/path/source（D-12）且不输出 token/secret。</action>
  <acceptance_criteria>- `src/lib.rs` 中默认初始化路径不再包含 `:memory:`。
- 存在路径来源（default/env）逻辑分支。
- 存在目录创建调用（如 `std::fs::create_dir_all` 或等价逻辑）。
- 日志包含 provider/path/source 且未输出 secret/token 值。</acceptance_criteria>
  <verify>
    <automated>cargo test -p native-tauri --lib -- --nocapture</automated>
  </verify>
  <done>桌面端默认启动链路已改为 Turso 文件库，且路径可观测、失败可快速暴露。</done>
</task>

<task type="auto">
  <name>Task 2: 对齐桌面命令链路与依赖声明到 Turso-only 默认面</name>
  <read_first>apps/client/native/src-tauri/Cargo.toml, packages/adapters/hosts/tauri/src/commands/counter.rs, packages/adapters/hosts/tauri/Cargo.toml</read_first>
  <files>apps/client/native/src-tauri/Cargo.toml, packages/adapters/hosts/tauri/src/commands/counter.rs</files>
  <action>检查并收敛桌面默认依赖图为 Turso-only（D-05/D-07）：确保 `native-tauri` 与 `runtime_tauri` 默认命令路径仅引用 Turso 适配器；若命令层存在隐式 fallback 到其他 adapter 或 memory，移除 fallback 并改为显式错误。保持 `storage_surreal` / `storage_libsql` 不进入默认桌面依赖链（D-06）。</action>
  <acceptance_criteria>- `native-tauri/Cargo.toml` 默认依赖链仍可编译且不新增 Surreal/libsql 依赖。
- counter/agent 相关命令路径无非 Turso fallback。
- `cargo check -p native-tauri` 通过。</acceptance_criteria>
  <verify>
    <automated>cargo check -p native-tauri</automated>
  </verify>
  <done>桌面端默认运行面仅依赖 Turso 路径，命令层不存在静默降级或跨 adapter 隐式耦合。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env→native runtime | 环境变量覆盖路径可能被滥用到不安全位置 |
| runtime→filesystem | 桌面进程写入本地 DB 文件，涉及权限与路径注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-03 | T | native/src-tauri/src/lib.rs path resolution | mitigate | canonicalize 路径并拒绝 memory 字面量作为默认路径 |
| T-10.1-04 | D | native startup migration | mitigate | 迁移失败立即退出，不继续以半初始化状态运行 |
| T-10.1-05 | I | startup logs | mitigate | 输出 provider/path/source 白名单字段，不记录 token/secret |
</threat_model>

<verification>
- `cargo test -p native-tauri --lib`
- `cargo check -p native-tauri`
</verification>

<success_criteria>
- native-tauri 默认路径不含 `:memory:`。
- 路径支持 env 覆盖且启动日志可观测来源。
- 默认桌面命令链路仅依赖 Turso。
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-02-SUMMARY.md`
</output>
