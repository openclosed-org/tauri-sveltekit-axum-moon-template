---
phase: 10.1-docs-storage-compile-requirements-md-turso-only
plan: 02
subsystem: database
tags: [tauri, rust, turso, sqlite, storage, runtime]

requires:
  - phase: 10-multi-tenant-repeatable-verification-channel
    provides: fixed tenant baseline and Turso adapter integration
provides:
  - native-tauri default startup path switched to file-backed Turso storage
  - path source observability contract (provider/path/source) on desktop startup
  - explicit Turso-only counter command service wiring in runtime_tauri
affects: [native-tauri, runtime_tauri, desktop-startup, storage-path-policy]

tech-stack:
  added: []
  patterns:
    - app-local-data database path resolution with env override
    - fail-fast startup for directory/path/migration failures

key-files:
  created:
    - .planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-02-SUMMARY.md
  modified:
    - apps/client/native/src-tauri/src/lib.rs
    - packages/adapters/hosts/tauri/src/commands/counter.rs

key-decisions:
  - "桌面端数据库默认落点使用 app_local_data_dir/runtime_tauri.db，禁止 memory 语义路径。"
  - "启动阶段输出 provider/path/source 白名单字段，不记录 secret/token 明文。"

patterns-established:
  - "路径解析先 create_dir_all 再 canonicalize，并统一 fail-fast。"
  - "命令层通过单一 Turso service 构造函数维持 adapter 收敛。"

requirements-completed: [MTEN-01]

duration: 19 min
completed: 2026-04-08
---

# Phase 10.1 Plan 02: native-tauri Turso-only 默认路径与命令链路收敛 Summary

**桌面端启动链路改为文件型 Turso 数据库并输出 provider/path/source 可观测信息，同时保持命令层仅走 Turso service 路径。**

## Performance

- **Duration:** 19 min
- **Started:** 2026-04-08T13:39:10Z
- **Completed:** 2026-04-08T13:58:10Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- 完成 Task 1（TDD）：为 native-tauri 新增路径解析测试并实现默认文件路径、env 覆盖、目录自动创建、startup 日志 source 标记。
- 启动阶段数据库初始化改为 fail-fast 文件库流程：路径解析、目录创建、连接、租户迁移、counter/agent 迁移均在失败时立即中止。
- 完成 Task 2：counter 命令层抽出 Turso service 构造函数，显式保持 Turso-only 分支，无其他 adapter fallback。

## Task Commits

1. **Task 1 (RED): native-tauri 默认 DB 路径改为文件库并支持来源标识** - `f97d0c7` (test)
2. **Task 1 (GREEN): native-tauri 默认 DB 路径改为文件库并支持来源标识** - `ef5d230` (feat)
3. **Task 2: 对齐桌面命令链路与依赖声明到 Turso-only 默认面** - `2fcffbe` (refactor)

## Files Created/Modified
- `apps/client/native/src-tauri/src/lib.rs` - 引入 DB 路径解析/来源标记/目录创建/memory 路径拒绝/启动日志字段输出，并将 EmbeddedTurso 初始化迁移到 setup 阶段。
- `packages/adapters/hosts/tauri/src/commands/counter.rs` - 抽出 `build_turso_counter_service`，统一命令层 Turso service 创建路径。

## Decisions Made
- 默认数据库路径采用 `app.path().app_local_data_dir()/runtime_tauri.db`，并允许 `NATIVE_TAURI_TURSO_DB_PATH` 覆盖。
- 路径解析统一 canonicalize 后再使用，拒绝 `:memory:`/`memory`/`mode=memory` 语义，避免非持久化启动。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] 拒绝 memory-like URL 语义路径**
- **Found during:** Task 1 (GREEN)
- **Issue:** 初版仅拒绝 `:memory:`/`memory` 字面量，`file:...mode=memory` 仍可能绕过持久化约束。
- **Fix:** 新增 `is_disallowed_memory_path`，统一拒绝 `:memory:`、`memory`、`mode=memory`。
- **Files modified:** `apps/client/native/src-tauri/src/lib.rs`
- **Verification:** `cargo test -p native-tauri --lib -- --nocapture` 通过（含新增拒绝测试）。
- **Committed in:** `ef5d230`

---

**Total deviations:** 1 auto-fixed (Rule 2: 1)
**Impact on plan:** 偏差属于 threat model 约束补强，无额外范围扩张。

## Authentication Gates

None.

## Issues Encountered
- 仓库存在预先未提交变更：`servers/api/src/config.rs`。执行中未纳入本计划提交，避免跨任务污染。

## Known Stubs

None.

## Next Phase Readiness
- 已满足本计划成功标准：默认路径非 memory、支持 env 来源标识、桌面命令链路保持 Turso-only。
- 可进入 10.1 后续计划继续收敛服务端路径与编译链路。

## Self-Check: PASSED

- FOUND: `.planning/phases/10.1-docs-storage-compile-requirements-md-turso-only/10.1-02-SUMMARY.md`
- FOUND: `f97d0c7`
- FOUND: `ef5d230`
- FOUND: `2fcffbe`
