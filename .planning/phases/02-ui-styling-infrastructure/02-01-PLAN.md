---
phase: 02-ui-styling-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-ui/vite.config.ts
  - apps/desktop-ui/src/app.css
autonomous: true
requirements:
  - UI-04

must_haves:
  truths:
    - "TailwindCSS v4 compiles without errors in vite dev and vite build"
    - "Custom theme tokens (primary color scale, gray scale) are available as Tailwind utilities"
    - "Dark mode CSS variables exist and switch when html.dark class is toggled"
    - "Custom breakpoints (sm:375, md:800, lg:1280) work in responsive utilities"
  artifacts:
    - path: "apps/desktop-ui/src/app.css"
      provides: "TailwindCSS v4 import, @theme block with design tokens, dark mode variables"
      contains: "@import \"tailwindcss\""
    - path: "apps/desktop-ui/vite.config.ts"
      provides: "@tailwindcss/vite plugin integration"
      contains: "tailwindcss()"
  key_links:
    - from: "apps/desktop-ui/vite.config.ts"
      to: "app.css"
      via: "@tailwindcss/vite plugin processes CSS"
      pattern: "import.*@tailwindcss/vite"
---

<objective>
Configure TailwindCSS v4 with Vite plugin integration and create the global CSS file with custom theme tokens and dark mode infrastructure.

Purpose: Foundation for the design system — all subsequent component wrappers depend on these CSS variables and Tailwind utilities.
Output: `app.css` with @theme tokens, `vite.config.ts` with Tailwind plugin.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ui-styling-infrastructure/02-CONTEXT.md
@.planning/phases/02-ui-styling-infrastructure/02-RESEARCH.md
@apps/desktop-ui/vite.config.ts
@apps/desktop-ui/src/app.html

<interfaces>
Current vite.config.ts:
```typescript
import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [sveltekit()],
});
```

Current app.html:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Tauri SvelteKit Axum Template</title>
    %sveltekit.head%
  </head>
  <body data-sveltekit-preload-data="hover">
    <div style="display: contents">%sveltekit.body%</div>
  </body>
</html>
```

@tailwindcss/vite is in devDependencies at ^4.0.0 in package.json.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app.css with TailwindCSS v4 theme</name>
  <files>apps/desktop-ui/src/app.css</files>
  <read_first>
    - apps/desktop-ui/src/app.html (current HTML structure)
    - .planning/phases/02-ui-styling-infrastructure/02-CONTEXT.md (design decisions D-01 through D-10)
    - .planning/phases/02-ui-styling-infrastructure/02-RESEARCH.md (TailwindCSS v4 @theme syntax)
  </read_first>
  <behavior>
    - Test 1: CSS file imports tailwindcss via @import "tailwindcss"
    - Test 2: @theme block defines --color-primary-{50-950} scale using oklch
    - Test 3: @theme block defines --color-gray-{50-950} using zinc palette
    - Test 4: @theme block defines custom breakpoints (sm:375px, md:800px, lg:1280px)
    - Test 5: :root defines CSS variables for both light and dark mode
    - Test 6: .dark class overrides CSS variables with dark palette
  </behavior>
  <action>
    Create `apps/desktop-ui/src/app.css` with the following structure:

    1. Import TailwindCSS v4:
    ```css
    @import "tailwindcss";
    ```

    2. Configure @theme block with design tokens per decisions D-01, D-02, D-03, D-09, D-10:
    ```css
    @theme {
      /* Primary: blue-indigo (Linear aesthetic) — per D-01 */
      --color-primary-50: oklch(97% 0.015 260);
      --color-primary-100: oklch(94% 0.03 260);
      --color-primary-200: oklch(88% 0.06 260);
      --color-primary-300: oklch(78% 0.1 260);
      --color-primary-400: oklch(68% 0.16 260);
      --color-primary-500: oklch(55% 0.2 260);
      --color-primary-600: oklch(48% 0.2 260);
      --color-primary-700: oklch(40% 0.18 260);
      --color-primary-800: oklch(33% 0.15 260);
      --color-primary-900: oklch(25% 0.12 260);
      --color-primary-950: oklch(18% 0.1 260);

      /* Gray: neutral zinc — per D-02 */
      --color-gray-50: oklch(98% 0.005 260);
      --color-gray-100: oklch(96% 0.005 260);
      --color-gray-200: oklch(92% 0.01 260);
      --color-gray-300: oklch(87% 0.015 260);
      --color-gray-400: oklch(70% 0.015 260);
      --color-gray-500: oklch(55% 0.015 260);
      --color-gray-600: oklch(45% 0.015 260);
      --color-gray-700: oklch(37% 0.015 260);
      --color-gray-800: oklch(27% 0.01 260);
      --color-gray-900: oklch(20% 0.01 260);
      --color-gray-950: oklch(14% 0.005 260);

      /* Typography: system fonts — per D-03 */
      --font-family-sans: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      --font-family-mono: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, monospace;

      /* Tauri-optimized breakpoints — per D-10 */
      --breakpoint-sm: 375px;
      --breakpoint-md: 800px;
      --breakpoint-lg: 1280px;
    }
    ```

    3. Add dark mode CSS overrides (class-based on html element — per D-04):
    ```css
    /* Dark mode overrides — per D-04 */
    :root {
      --color-bg: var(--color-gray-50);
      --color-bg-elevated: white;
      --color-text: var(--color-gray-900);
      --color-text-muted: var(--color-gray-500);
      --color-border: var(--color-gray-200);
      --color-border-hover: var(--color-gray-300);
    }

    :root.dark {
      --color-bg: var(--color-gray-950);
      --color-bg-elevated: var(--color-gray-900);
      --color-text: var(--color-gray-50);
      --color-text-muted: var(--color-gray-400);
      --color-border: var(--color-gray-800);
      --color-border-hover: var(--color-gray-700);
    }

    /* Base body styles */
    body {
      background-color: var(--color-bg);
      color: var(--color-text);
      font-family: var(--font-family-sans);
    }
    ```

    4. Add base layer styles for component consistency:
    ```css
    @layer base {
      * {
        border-color: var(--color-border);
      }
    }
    ```

    Do NOT add any tailwind.config.js — TailwindCSS v4 uses CSS-only config (per D-08).
    Do NOT import custom fonts — system fonts only (per D-03).
  </action>
  <verify>
    <automated>cd apps/desktop-ui && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>app.css exists with @import "tailwindcss", @theme block defines primary + gray color scales, custom breakpoints, system font stack, and dark mode CSS variable overrides</done>
</task>

<task type="auto">
  <name>Task 2: Add @tailwindcss/vite plugin to vite.config.ts</name>
  <files>apps/desktop-ui/vite.config.ts</files>
  <read_first>
    - apps/desktop-ui/vite.config.ts (current config)
    - apps/desktop-ui/package.json (verify @tailwindcss/vite in devDependencies)
  </read_first>
  <action>
    Update `apps/desktop-ui/vite.config.ts` to add the `@tailwindcss/vite` plugin BEFORE the `sveltekit()` plugin:

    ```typescript
    import { sveltekit } from '@sveltejs/kit/vite';
    import tailwindcss from '@tailwindcss/vite';
    import { defineConfig } from 'vite';

    export default defineConfig({
      plugins: [tailwindcss(), sveltekit()],
    });
    ```

    The tailwindcss plugin MUST come before sveltekit() in the plugins array — CSS processing order matters for v4.
  </action>
  <verify>
    <automated>cd apps/desktop-ui && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>vite.config.ts imports @tailwindcss/vite and includes it in plugins array before sveltekit()</done>
</task>

</tasks>

<verification>
1. `cd apps/desktop-ui && npx vite build` succeeds without CSS errors
2. `apps/desktop-ui/src/app.css` contains `@import "tailwindcss"`
3. `apps/desktop-ui/src/app.css` contains `@theme` block with `--color-primary-500`
4. `apps/desktop-ui/vite.config.ts` imports `@tailwindcss/vite`
5. `apps/desktop-ui/vite.config.ts` has `tailwindcss()` before `sveltekit()` in plugins
</verification>

<success_criteria>
- TailwindCSS v4 compiles with custom theme tokens
- Dark mode CSS variables defined for both light and dark states
- Custom Tauri-optimized breakpoints (375, 800, 1280) configured
- Vite build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-styling-infrastructure/02-01-SUMMARY.md`
</output>
