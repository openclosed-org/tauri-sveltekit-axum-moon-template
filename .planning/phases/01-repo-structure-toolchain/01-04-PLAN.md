---
phase: 01-repo-structure-toolchain
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified: []
autonomous: false
requirements:
  - STRUCT-01
  - TOOL-01

must_haves:
  truths:
    - "Directory tree matches blueprint (verified by automated check)"
    - "just --list shows all expected commands"
    - "moon can parse moon.yml and .moon/workspace.yml"
    - "Cargo workspace builds"
    - "repo:doctor reports toolchain status"
  artifacts:
    - path: "Directory structure"
      provides: "Verified blueprint alignment"
    - path: "Toolchain commands"
      provides: "Working dev/verify/test entry points"
  key_links:
    - from: "Blueprint 02-repo-structure.md"
      to: "Actual directory tree"
      via: "diff comparison"
      pattern: "directory alignment check"
---

# Plan 04: Integration Verification

**Phase:** 01-repo-structure-toolchain
**Objective:** Verify all Phase 1 artifacts work together — directory structure, moon tasks, Justfile, toolchain.

**Purpose:** Catch integration issues before moving to Phase 2. This plan has a checkpoint for human verification.
**Output:** Verified working development environment entry points.

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repo-structure-toolchain/01-01-SUMMARY.md
@.planning/phases/01-repo-structure-toolchain/01-02-SUMMARY.md
@.planning/phases/01-repo-structure-toolchain/01-03-SUMMARY.md
@docs/blueprints/agent-native-starter-v1/02-repo-structure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification suite</name>
  <read_first>
    - docs/blueprints/agent-native-starter-v1/02-repo-structure.md (expected directories)
    - moon.yml (task definitions)
    - Justfile (commands)
  </read_first>
  <files></files>
  <action>
    Run the following verification checks and collect results:

    ```bash
    echo "=== Phase 1 Verification Suite ==="

    echo ""
    echo "1. Blueprint Directory Alignment"
    # Check each top-level dir from blueprint
    for d in apps servers workers packages crates tools .agents; do
      test -d "$d" && echo "  ✓ $d/" || echo "  ✗ $d/ MISSING"
    done

    echo ""
    echo "2. Key Subdirectories"
    for d in \
      workers/protocols workers/chains workers/jobs \
      tools/scripts tools/generators tools/mcp tools/evals \
      apps/ops apps/client/web/hosts \
      packages/contracts/auth packages/contracts/errors packages/contracts/ui packages/contracts/codegen \
      packages/adapters/auth packages/adapters/telemetry \
      packages/shared/env packages/shared/testing \
      packages/ui/icons packages/ui/tokens \
      .agents/prompts .agents/playbooks .agents/rubrics; do
      test -d "$d" && echo "  ✓ $d/" || echo "  ✗ $d/ MISSING"
    done

    echo ""
    echo "3. Configuration Files"
    test -f .prototools && echo "  ✓ .prototools" || echo "  ✗ .prototools MISSING"
    test -f moon.yml && echo "  ✓ moon.yml" || echo "  ✗ moon.yml MISSING"
    test -f Justfile && echo "  ✓ Justfile" || echo "  ✗ Justfile MISSING"
    test -f Cargo.toml && echo "  ✓ Cargo.toml" || echo "  ✗ Cargo.toml MISSING"
    test -f .moon/workspace.yml && echo "  ✓ .moon/workspace.yml" || echo "  ✗ .moon/workspace.yml MISSING"

    echo ""
    echo "4. Moon Tasks"
    grep -c "repo:" moon.yml | xargs -I{} echo "  repo:* tasks: {}"

    echo ""
    echo "5. Just Commands"
    just --list 2>&1 | grep -c "just " | xargs -I{} echo "  just commands: {}"

    echo ""
    echo "6. Cargo Workspace"
    cargo check --workspace 2>&1 | tail -3

    echo ""
    echo "=== Verification Complete ==="
    ```
  </action>
  <verify>
    <automated>
      echo "=== Verification ==="
      # All top-level dirs
      for d in apps servers workers packages crates tools .agents; do
        test -d "$d" || echo "FAIL: $d missing"
      done
      # Key configs
      test -f .prototools || echo "FAIL: .prototools missing"
      test -f moon.yml || echo "FAIL: moon.yml missing"
      test -f Justfile || echo "FAIL: Justfile missing"
      # Moon task count
      task_count=$(grep -c "repo:" moon.yml 2>/dev/null || echo 0)
      test "$task_count" -ge 20 || echo "FAIL: only $task_count repo: tasks (need 20+)"
      # Cargo
      cargo check --workspace 2>&1 | grep -q "error" && echo "FAIL: cargo errors" || echo "CARGO_OK"
      echo "DONE"
    </automated>
  </verify>
  <done>All verification checks pass. Directory tree aligns with blueprint. Moon has 20+ repo:* tasks. Cargo builds. Justfile has 12+ commands.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human verification of Phase 1 artifacts</name>
  <files></files>
  <action>Wait for human verification — display checklist and pause execution</action>
  <what-built>Phase 1 complete: directory scaffold, moon task graph, Justfile, .prototools</what-built>
  <how-to-verify>
    1. Run `just --list` — verify you see setup, dev, verify, test, typegen commands
    2. Run `just doctor` — verify it reports toolchain status
    3. Run `cargo check --workspace` — verify it compiles
    4. Run `tree -L 2 -d` (or ls) — verify directory structure matches your expectations
    5. Check `workers/`, `tools/`, `apps/ops/` exist with subdirectories
  </how-to-verify>
  <verify>Human confirms all checks pass by typing "approved"</verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <done>Human verification approved</done>
</task>

</tasks>

<verification>
- Automated checks pass (directories, configs, moon tasks, cargo build)
- Human confirms just commands work and structure looks correct
</verification>

<success_criteria>
- [ ] All blueprint directories exist on disk
- [ ] moon.yml has 20+ repo:* tasks in 6 categories
- [ ] Justfile has 12+ commands delegating to moon
- [ ] .prototools with bun + node
- [ ] Cargo workspace builds without errors
- [ ] Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-structure-toolchain/01-04-SUMMARY.md`
</output>
