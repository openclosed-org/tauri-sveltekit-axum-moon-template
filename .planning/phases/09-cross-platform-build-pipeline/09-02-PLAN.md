---
phase: 09-cross-platform-build-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - BUILD-02
must_haves:
  truths:
    - "CI 会在 push 到 main 与 pull_request 自动触发"
    - "CI 在 ubuntu/windows/macos 三个平台都执行构建验证"
    - "Linux runner 安装 Tauri 所需系统依赖后再构建"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "三平台 matrix 质量门禁与构建验证"
      contains: "strategy.matrix.os, push/main, pull_request"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "apps/desktop-ui/src-tauri/Cargo.toml"
      via: "cargo build --manifest-path"
      pattern: "cargo build --manifest-path apps/desktop-ui/src-tauri/Cargo.toml"
---

<objective>
把当前手动触发、单平台的 CI 改为三平台自动验证矩阵，确保跨平台构建从“人工约定”变成“每次提交都被验证”。

Purpose: 兑现 D-01/D-02/D-03/D-04/D-12/D-13 与 BUILD-02，锁定 Phase 9 核心交付。
Output: 一份自动触发、三平台、含构建校验步骤的 GitHub Actions 工作流。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-platform-build-pipeline/09-CONTEXT.md
@.github/workflows/ci.yml
@apps/desktop-ui/moon.yml
@apps/desktop-ui/src-tauri/Cargo.toml

<interfaces>
From current `.github/workflows/ci.yml` baseline:
```yaml
on:
  workflow_dispatch:
jobs:
  quality:
    runs-on: ubuntu-latest
```

Target build command contract (from desktop tauri crate):
```bash
cargo build --manifest-path apps/desktop-ui/src-tauri/Cargo.toml
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将 CI 改为三平台自动触发 matrix（per D-01/D-02/D-03）</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/09-cross-platform-build-pipeline/09-CONTEXT.md
    - apps/desktop-ui/moon.yml
  </read_first>
  <action>
    重写触发与 job 结构，确保不是“在原 job 上打补丁”：
    1) `on` 改为：
       - `push.branches: [main]`
       - `pull_request`（全部目标分支可触发）
       - 可保留 `workflow_dispatch` 作为手动兜底；
    2) 单 job 使用 `strategy.matrix.os: [ubuntu-latest, windows-latest, macos-latest]`；
    3) 统一工具链步骤：checkout → Rust stable → rust cache → Bun 1.3.11（D-03）；
    4) Linux 仅在 `runner.os == 'Linux'` 时安装系统依赖（D-12），继续使用 apt 模式，不引入 docker image；
    5) 阶段内明确不接入 release/upload/notarization 相关 step（D-05/D-06）。
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` 包含 `push` 且 `branches` 含 `main`
    - `.github/workflows/ci.yml` 包含 `pull_request`
    - workflow 包含 `strategy.matrix.os` 且同时含 `ubuntu-latest`、`windows-latest`、`macos-latest`
    - workflow 包含 `oven-sh/setup-bun@v2` 且 `bun-version: 1.3.11`
    - Linux apt 安装步骤包含 `libwebkit2gtk-4.1-dev` 与 `libgtk-3-dev`
    - 文件中不出现 `upload-artifact`、`release-plz`、`notarize`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/ci.yml','utf8');['push:','pull_request:','ubuntu-latest','windows-latest','macos-latest','oven-sh/setup-bun@v2','bun-version: 1.3.11','libwebkit2gtk-4.1-dev','libgtk-3-dev'].forEach(s=>{if(!y.includes(s))throw new Error('missing '+s)});['upload-artifact','release-plz','notarize'].forEach(s=>{if(y.includes(s))throw new Error('out of scope '+s)});"</automated>
  </verify>
  <done>CI 会在 push/PR 自动并行跑三平台，且保持 Bun+Rust 一致工具链。</done>
</task>

<task type="auto">
  <name>Task 2: 在每个平台 job 中加入构建验证链路（per D-04/D-13 + BUILD-02）</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - apps/desktop-ui/src-tauri/Cargo.toml
    - apps/desktop-ui/moon.yml
  </read_first>
  <action>
    在 matrix job 中补齐统一执行链（不得省略 build 校验）：
    1) Rust quality gates：
       - `cargo check --workspace --exclude desktop-ui-tauri`
       - `cargo fmt --all -- --check`
       - `cargo clippy --workspace --exclude desktop-ui-tauri -- -D warnings`
       - `cargo test --workspace --exclude desktop-ui-tauri`
    2) Frontend gates（`apps/desktop-ui`）：
       - `bun install --frozen-lockfile`
       - `bun run check`
       - `bun run lint`
       - `bun run build`
    3) Tauri crate build verification（关键）：
       - `cargo build --manifest-path apps/desktop-ui/src-tauri/Cargo.toml`
    4) 保持 native runner 编译，不引入 cross target 参数（D-13）。
  </action>
  <acceptance_criteria>
    - workflow 包含 `cargo check --workspace --exclude desktop-ui-tauri`
    - workflow 包含 `bun run check` 与 `bun run lint` 与 `bun run build`
    - workflow 包含 `cargo build --manifest-path apps/desktop-ui/src-tauri/Cargo.toml`
    - workflow 不包含 `--target x86_64-unknown-linux-gnu` 之类 cross-compilation 参数
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/ci.yml','utf8');['cargo check --workspace --exclude desktop-ui-tauri','bun run check','bun run lint','bun run build','cargo build --manifest-path apps/desktop-ui/src-tauri/Cargo.toml'].forEach(s=>{if(!y.includes(s))throw new Error('missing '+s)});if(/--target\s+\S+/.test(y))throw new Error('cross target detected');"</automated>
  </verify>
  <done>CI 每个平台都会执行完整质量门禁 + Tauri 构建验证，满足 BUILD-02 的“Windows/macOS/Linux 均可构建”。</done>
</task>

</tasks>

<verification>
检查 `.github/workflows/ci.yml` 文本中所有关键触发器、matrix 平台、构建命令和越界关键词，保证 plan 可被静态验证。
</verification>

<success_criteria>
- CI 触发策略变为 push/main + pull_request
- matrix 覆盖三平台 runner
- 每个平台执行同构质量门禁与 tauri build 验证
- 无 release/artifact/notarization 越界配置
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-platform-build-pipeline/09-02-SUMMARY.md`
</output>
