---
phase: 09-cross-platform-build-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-ui/src-tauri/tauri.conf.json
  - apps/desktop-ui/src-tauri/Entitlements.plist
autonomous: true
requirements:
  - BUILD-02
must_haves:
  truths:
    - "macOS 打包配置包含可解析的 entitlements 文件引用"
    - "Windows 打包配置明确使用 NSIS 默认安装器路径与 WebView2 bootstrapper 策略"
    - "Tauri 仍保持 bundle.targets = all，三平台都走同一份配置"
  artifacts:
    - path: "apps/desktop-ui/src-tauri/tauri.conf.json"
      provides: "Windows NSIS + macOS entitlements 的跨平台 bundle 配置"
      contains: "bundle.windows.nsis, bundle.windows.webviewInstallMode, bundle.macOS.entitlements, bundle.targets=all"
    - path: "apps/desktop-ui/src-tauri/Entitlements.plist"
      provides: "macOS hardened runtime 基础 entitlements"
      contains: "com.apple.security.network.client"
  key_links:
    - from: "apps/desktop-ui/src-tauri/tauri.conf.json"
      to: "apps/desktop-ui/src-tauri/Entitlements.plist"
      via: "bundle.macOS.entitlements path"
      pattern: "\"entitlements\": \"./Entitlements.plist\""
---

<objective>
落地 Tauri 跨平台打包基线配置（Windows NSIS + macOS entitlements），确保同一份 `tauri.conf.json` 能覆盖 Phase 9 的平台打包前置条件。

Purpose: 兑现 D-07/D-08/D-09/D-10，把“boilerplate-ready defaults”变成可执行配置，而不是口头约定。
Output: 可被 `cargo tauri build` 使用的 NSIS + macOS entitlements 配置与 entitlements 文件。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-platform-build-pipeline/09-CONTEXT.md
@apps/desktop-ui/src-tauri/tauri.conf.json

<interfaces>
From `apps/desktop-ui/src-tauri/tauri.conf.json`:
```json
{
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": ["..."]
  }
}
```

From Tauri v2 docs research:
```json
{
  "bundle": {
    "macOS": {
      "entitlements": "./Entitlements.plist"
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 配置 Windows NSIS + WebView2 策略与 macOS entitlements（per D-07/D-08/D-09/D-10/D-11）</name>
  <files>apps/desktop-ui/src-tauri/tauri.conf.json, apps/desktop-ui/src-tauri/Entitlements.plist</files>
  <read_first>
    - apps/desktop-ui/src-tauri/tauri.conf.json
    - .planning/phases/09-cross-platform-build-pipeline/09-CONTEXT.md
    - https://v2.tauri.app/distribute/windows-installer/
    - https://v2.tauri.app/distribute/macos-application/
  </read_first>
  <action>
    在 `bundle` 下写入明确平台配置，禁止“保持默认”这种模糊实现：
    1) 保留 `"targets": "all"`（D-10，且不得改为显式平台列表）；
    2) 新增 `bundle.windows.nsis`，至少包含：
       - `"installMode": "currentUser"`
       - `"displayLanguageSelector": false`
       - 不引入 MSI 或双安装器并行（D-08）；
    3) 显式新增 `bundle.windows.webviewInstallMode`：
       - `"type": "downloadBootstrapper"`（D-11，默认 bootstrapper 策略）
       - 不配置 fixedRuntime/offline bundling 路径；
    4) 新增 `bundle.macOS`，至少包含：
       - `"hardenedRuntime": true`
       - `"entitlements": "./Entitlements.plist"`（相对 `src-tauri`）
       - 不接入 notarization/signing workflow（D-09 + D-05/D-06）；
    5) 新建 `apps/desktop-ui/src-tauri/Entitlements.plist`，写入基础 key（boilerplate 级别）：
       - `com.apple.security.network.client = true`
       - `com.apple.security.cs.allow-jit = true`
       - `com.apple.security.cs.allow-unsigned-executable-memory = true`
    6) 不添加发布、上传 artifacts、签名自动化字段（Deferred + D-06）。
  </action>
  <acceptance_criteria>
    - `apps/desktop-ui/src-tauri/tauri.conf.json` 包含 `"targets": "all"`
    - JSON 中存在 `bundle.windows.nsis.installMode` 且值为 `currentUser`
    - JSON 中存在 `bundle.windows.webviewInstallMode.type` 且值为 `downloadBootstrapper`
    - JSON 中存在 `bundle.macOS.entitlements` 且值为 `./Entitlements.plist`
    - `apps/desktop-ui/src-tauri/Entitlements.plist` 存在并包含 `com.apple.security.network.client`
    - 文件中不包含 `notarization`、`artifact`、`release` 相关自动化配置
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const c=JSON.parse(fs.readFileSync('apps/desktop-ui/src-tauri/tauri.conf.json','utf8'));if(c.bundle.targets!=='all')throw new Error('targets not all');if(c.bundle?.windows?.nsis?.installMode!=='currentUser')throw new Error('nsis installMode');if(c.bundle?.windows?.webviewInstallMode?.type!=='downloadBootstrapper')throw new Error('webviewInstallMode');if(c.bundle?.macOS?.entitlements!=='./Entitlements.plist')throw new Error('entitlements path');const p=fs.readFileSync('apps/desktop-ui/src-tauri/Entitlements.plist','utf8');['com.apple.security.network.client','com.apple.security.cs.allow-jit','com.apple.security.cs.allow-unsigned-executable-memory'].forEach(k=>{if(!p.includes(k))throw new Error('missing '+k)});"</automated>
  </verify>
  <done>tauri.conf.json 的跨平台 bundle 配置可直接用于后续 CI 三平台构建校验，且符合本阶段“仅构建验证、不做发布自动化”的边界。</done>
</task>

</tasks>

<verification>
验证 `tauri.conf.json` 与 `Entitlements.plist` 的键值可被脚本解析，避免“看起来改了但键名错误”导致 CI 后续失败。
</verification>

<success_criteria>
- Windows NSIS 与 macOS entitlements 均有明确配置项且路径可解析
- `bundle.targets` 维持 `all`
- 未引入发布/签名自动化越界内容
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-platform-build-pipeline/09-01-SUMMARY.md`
</output>
