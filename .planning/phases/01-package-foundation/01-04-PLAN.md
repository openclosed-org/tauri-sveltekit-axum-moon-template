---
phase: 01-package-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
files_modified: []
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - BUILD-03
user_setup: []
must_haves:
  truths:
    - "All Phase 1 requirements (PKG-01, PKG-02, PKG-03, BUILD-03) are satisfied"
    - "Cargo workspace resolves without compilation errors"
    - "moon lint and test tasks are configured for parallel execution"
  artifacts: []
  key_links: []
---

# Phase 1, Plan 04: Verification Gate

## Objective

Verify all Phase 1 package foundation changes are correct and the workspace resolves cleanly.

**Purpose:** Catch dependency resolution failures, version conflicts, and missing declarations before Phase 2 begins. Phase 1 is a prerequisite for all other phases — errors here cascade everywhere.

**Output:** Verification results confirming all requirements are met.

## Context

After Plans 01-03 complete, the following should be true:
- `apps/desktop-ui/package.json` has all frontend deps per TECH_SELECTION.md
- Root `Cargo.toml` has all workspace deps with pinned versions and release profile
- `apps/desktop-ui/src-tauri/Cargo.toml` has all 7 Tauri plugins
- `moon.yml` already has lint/test aggregation (verified existing)

<task type="auto">
  <name>Task 1: Verify Cargo workspace resolution</name>
  <files>Cargo.toml, apps/desktop-ui/src-tauri/Cargo.toml</files>
  <read_first>
    - Cargo.toml
    - apps/desktop-ui/src-tauri/Cargo.toml
  </read_first>
  <action>
    Run `cargo check` to verify all workspace dependencies resolve correctly.
    This validates:
    - All workspace dependencies in root Cargo.toml are valid crate names with valid versions
    - All `{ workspace = true }` references in crate Cargo.toml files resolve
    - No version conflicts between workspace members
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>cargo check exits with code 0 — all workspace dependencies resolve</done>
</task>

<task type="auto">
  <name>Task 2: Verify moon workspace tasks</name>
  <files>moon.yml</files>
  <read_first>
    - moon.yml
  </read_first>
  <action>
    Run `moon ci` or `moon run lint --dry` to verify moon can resolve the parallel task graph.
    This validates:
    - lint task depends on all sub-project lint tasks
    - test task depends on all sub-project test tasks
    - Tasks are scheduled for parallel execution
  </action>
  <verify>
    <automated>moon run lint --dry 2>&1 | head -20</automated>
  </verify>
  <done>moon dry-run shows parallel task execution graph without errors</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Phase 1 complete: all package dependencies configured</what-built>
  <how-to-verify>
    1. Run `cargo check` — should resolve all workspace deps without errors
    2. Run `cat apps/desktop-ui/package.json` — verify bits-ui, @lucide/svelte, @pqoqubbw/icons, vitest, @playwright/test present
    3. Run `cat Cargo.toml` — verify all 7 tauri-plugin-* entries, axum, surrealdb, [profile.release] present
    4. Run `cat apps/desktop-ui/src-tauri/Cargo.toml` — verify all 7 plugin references
    5. Run `moon run lint --dry` — verify parallel execution graph
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
