---
phase: 01-package-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-ui/package.json
autonomous: true
requirements:
  - PKG-01
  - PKG-02
user_setup: []
must_haves:
  truths:
    - "package.json declares bits-ui 2.16.4 as a dependency"
    - "package.json declares @lucide/svelte, @pqoqubbw/icons, @lottiefiles/svelte-lottie-player as commented-out optional deps with exact versions"
    - "package.json declares vitest, vitest-browser-svelte, @playwright/test, maestro as devDependencies"
    - "package.json dev script runs both vite dev and cargo tauri dev concurrently"
  artifacts:
    - path: "apps/desktop-ui/package.json"
      provides: "Frontend dependency declarations"
      contains: "bits-ui", "@lucide/svelte", "@pqoqubbw/icons", "vitest", "@playwright/test"
  key_links:
    - from: "apps/desktop-ui/package.json"
      to: "apps/desktop-ui/src/"
      via: "import statements in Svelte components"
      pattern: "import.*from.*bits-ui"
---

# Phase 1, Plan 01: Frontend Package Dependencies

## Objective

Align `apps/desktop-ui/package.json` with TECH_SELECTION.md frontend specifications: exact dependency versions, correct optional dependency names, dev/test tooling, and Tauri dev script.

**Purpose:** Package.json is the single source of truth for frontend dependencies. Incomplete or incorrect entries cause build failures and missing component imports in later phases.

**Output:** Updated `apps/desktop-ui/package.json` with all frontend dependencies per TECH_SELECTION.md.

## Context

Current `apps/desktop-ui/package.json` state:
- Has `bits-ui: "^2.16.4"` — version OK but caret range, TECH_SELECTION specifies `2.16.4`
- Has commented-out deps with **wrong names**: `lucide-animated` (should be `@lucide/svelte`), `lottieplayer` (should be `@lottiefiles/svelte-lottie-player`)
- Missing `@pqoqubbw/icons` entirely
- Missing test devDependencies: `vitest`, `vitest-browser-svelte`, `@playwright/test`, `maestro`
- Missing `concurrently` for dev script
- Dev script only runs `vite dev`, not `cargo tauri dev` concurrently

Per TECH_SELECTION.md §3.1 (已验证 2026-03-28):
```
dependencies: bits-ui 2.16.4, tailwindcss 4.2.2, @lucide/svelte 1.7.0, @pqoqubbw/icons latest, @lottiefiles/svelte-lottie-player 0.3.1
devDependencies: @sveltejs/kit 2.55.0, svelte 5.55.0, @sveltejs/adapter-static 3.0.0, vite 8.0.3, vitest 4.1.2, vitest-browser-svelte latest, @playwright/test 1.58.2, maestro latest, @biomejs/biome 1.9.4, typescript 5.5.0
scripts.dev: concurrently "vite dev" "cargo tauri dev"
```

<task type="auto">
  <name>Task 1: Align frontend dependencies in package.json</name>
  <files>apps/desktop-ui/package.json</files>
  <read_first>
    - apps/desktop-ui/package.json (current state)
  </read_first>
  <action>
    Update `apps/desktop-ui/package.json` to match TECH_SELECTION.md §3.1 exactly:

    1. **dependencies** — Update and add:
       - `"bits-ui": "2.16.4"` (remove caret, pin exact)
       - `"tailwindcss": "4.2.2"` (pin exact, currently `"^4.0.0"`)
       - `"@lucide/svelte": "1.7.0"` (new — main static icon set)
       - `"@pqoqubbw/icons": "latest"` (new — animated icons, user specified)
       - `"@lottiefiles/svelte-lottie-player": "0.3.1"` (new — Lottie animations)

    2. **Remove** the broken commented-out block:
       ```json
       // "vitepress": "^1.x",
       // "lucide-animated": "^0.x",
       // "lottieplayer": "^1.x"
       ```
       This is invalid JSON (comments not allowed). Remove entirely.

    3. **devDependencies** — Pin existing versions and add missing:
       - `"@sveltejs/kit": "2.55.0"` (pin, currently `"^2.50.0"`)
       - `"svelte": "5.55.0"` (pin, currently `"^5.54.0"`)
       - `"vite": "8.0.3"` (pin, currently `"^8.0.0"`)
       - `"@biomejs/biome": "1.9.4"` (pin, currently `"^1.9.4"`)
       - `"typescript": "5.5.0"` (pin, currently `"^5.5.0"`)
       - `"@sveltejs/adapter-static": "3.0.0"` (pin, currently `"^3.0.0"`)
       - ADD: `"vitest": "4.1.2"`
       - ADD: `"vitest-browser-svelte": "latest"`
       - ADD: `"@playwright/test": "1.58.2"`
       - ADD: `"maestro": "latest"`

    4. **scripts** — Add `dev:tauri` and `test` scripts:
       - Add: `"dev:tauri": "concurrently \"vite dev\" \"cargo tauri dev\""`
       - Add: `"test:unit": "vitest run"`
       - Add: `"test:e2e": "playwright test"`
       - Add: `"test:mobile": "maestro test tests/flow/"`

    5. **Add** `"concurrently"` to devDependencies: `"concurrently": "latest"`

    Keep existing `"@sveltejs/vite-plugin-svelte": "^5.0.0"`, `"@tailwindcss/vite": "^4.0.0"`, `"svelte-check": "^4.0.0"` unchanged.
  </action>
  <verify>
    <automated>cd apps/desktop-ui && node -e "const p=require('./package.json'); const checks=['bits-ui' in p.dependencies && p.dependencies['bits-ui']==='2.16.4','@lucide/svelte' in p.dependencies,'@pqoqubbw/icons' in p.dependencies,'@lottiefiles/svelte-lottie-player' in p.dependencies,'vitest' in p.devDependencies,'@playwright/test' in p.devDependencies,'maestro' in p.devDependencies,'concurrently' in p.devDependencies,typeof p.scripts['dev:tauri']===\"string\"&&p.scripts['dev:tauri'].includes('concurrently'),typeof p.scripts['test:unit']===\"string\"&&p.scripts['test:unit'].includes('vitest'),!JSON.stringify(p.dependencies).includes('lucide-animated'),!JSON.stringify(p.dependencies).includes('lottieplayer')]; console.log(checks.every(Boolean)?'PASS':'FAIL: '+checks.map((c,i)=>c?null:'check-'+i).filter(Boolean).join(', '))"</automated>
  </verify>
  <done>package.json has all frontend dependencies per TECH_SELECTION.md with correct names and pinned versions; broken commented-out block removed; test/dev scripts added</done>
</task>
