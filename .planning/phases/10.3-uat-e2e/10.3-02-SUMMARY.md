---
phase: "10.3-uat-e2e"
plan: "10.3-02"
subsystem: "api-routes, frontend, e2e-tests"
tags: [route-prefix, auth-header, css-fix, e2e]
dependencies:
  requires: ["10.3-01"]
  provides: ["api-route-alignment", "counter-auth", "admin-visibility"]
  affects: ["servers/api", "apps/client/web", "e2e-desktop-playwright"]
tech-stack:
  added: []
  patterns: ["Axum route prefix alignment", "localStorage Bearer token injection", "Tailwind dark mode tokens"]
key-files:
  created: []
  modified:
    - servers/api/src/routes/admin.rs
    - servers/api/src/routes/agent.rs
    - apps/client/web/app/src/routes/(app)/counter/+page.svelte
    - apps/client/web/app/src/routes/(app)/admin/+page.svelte
    - apps/client/web/app/tests/e2e/tenant-isolation.test.ts
    - e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts
decisions: []
metrics:
  completed: "2026-04-09"
  tasks: 5
  files: 6
---

# Phase 10.3 Plan 02: API 路由前缀统一 + Counter auth header Summary

## One-liner

统一 admin/agent 路由 `/api` 前缀、counter fetch 添加 Authorization header、admin 页面 CSS 修复、E2E 测试 URL 同步

## What Changed

### Task 1: 修复 admin 路由前缀
- `servers/api/src/routes/admin.rs` — `/admin/stats` → `/api/admin/stats`
- 解决前端 `http://localhost:3001/api/admin/stats` 请求 404 问题
- **Commit:** f355b0a

### Task 2: 修复 agent 路由前缀
- `servers/api/src/routes/agent.rs` — 三个路由全部加 `/api` 前缀：
  - `/agent/conversations` → `/api/agent/conversations`
  - `/agent/conversations/{id}/messages` → `/api/agent/conversations/{id}/messages`
  - `/agent/chat` → `/api/agent/chat`
- **Commit:** a1f0393

### Task 3: Counter fetch 添加 Authorization header
- `apps/client/web/app/src/routes/(app)/counter/+page.svelte`
- `invokeCommand()` 函数：从 `localStorage.getItem('auth_id_token')` 读取 token
- fetch 调用时加入 `Authorization: Bearer <token>` header
- 解决 web 模式 counter 增减/重置 401 问题
- **Commit:** a92aef9

### Task 4: Admin 页面 CSS 修复
- `apps/client/web/app/src/routes/(app)/admin/+page.svelte`
- `text-[var(--color-text)]` → `text-gray-900 dark:text-gray-50`
- `text-[var(--color-text-muted)]` → `text-gray-500 dark:text-gray-400`
- 解决 admin dashboard 文字不可见问题（白色文字在白色背景上）
- **Commit:** 0c3008f

### Task 5: 更新 E2E 测试 agent URL
- `apps/client/web/app/tests/e2e/tenant-isolation.test.ts` — `AGENT_CONVERSATIONS_URL` 更新
- `e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts` — `AGENT_CONVERSATIONS_URL` 更新
- **Commit:** ddb494c

## Verification Performed

- 5 个 task 全部原子提交，commit hash 记录于上表
- `git status` 干净，无未跟踪文件
- 无 stub 模式（TODO/FIXME/placeholder）存在于修改文件中

## Remaining Risks

- **Rust 编译验证未执行：** 由于环境限制未运行 `cargo check -p api`，需在 CI 或本地验证路由表无冲突
- **Web 模式端到端验证未执行：** 需手动在浏览器中验证 counter/admin/agent 功能正常

## Deviations from Plan

None — plan executed exactly as written.

## Threat Flags

None — changes are route prefix alignment, header injection, and CSS token replacement. No new network endpoints or auth paths introduced.

## Self-Check: PASSED
