---
phase: 10.3-uat-e2e
plan: 01
subsystem: runtime-abstraction
tags: [tauri, svelte, runtime-detection, web-compat, ipc]

requires:
  - phase: null
    provides: null
provides:
  - Centralized runtime abstraction (bridge.ts) eliminating top-level Tauri imports
  - Web mode startup without crashes on window.__TAURI_INTERNALS__
  - Tauri-desktop mode fully preserved with dynamic import pattern
affects:
  - All future IPC integrations must use bridge.ts instead of direct Tauri imports
  - E2E testing in browser mode now possible

tech-stack:
  added: []
  patterns:
    - "Runtime bridge pattern: isTauri() + safeInvoke/safeListen/safeChannel with dynamic imports"
    - "Guarded Tauri-only code paths with isTauri() checks at call sites"

key-files:
  created:
    - apps/client/web/app/src/lib/ipc/bridge.ts
  modified:
    - apps/client/web/app/src/routes/+layout.svelte
    - apps/client/web/app/src/lib/stores/auth.svelte.ts
    - apps/client/web/app/src/lib/ipc/auth.ts
    - apps/client/web/app/src/lib/ipc/agent.ts

key-decisions:
  - "Dynamic import (@tauri-apps/api/* via await import()) instead of conditional require — preserves ESM tree-shaking and matches project module type"
  - "clearAuthStore uses localStorage fallback in web mode instead of failing on plugin-store"
  - "safeChannel returns null in web mode — callers must handle null"

patterns-established:
  - "Bridge pattern: single module owns all dynamic Tauri imports, all consumers use typed safeXxx wrappers"

requirements-completed: [UAT-02]

duration: 3min
completed: 2026-04-09
---

# Phase 10.3 Plan 01: Web/Desktop Runtime Abstraction + Tauri Import Fix Summary

**Runtime bridge module eliminating all top-level @tauri-apps imports, enabling crash-free web startup via dynamic imports and guarded code paths**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-09T13:48:20Z
- **Completed:** 2026-04-09T13:51:53Z
- **Tasks:** 5
- **Files modified:** 5

## Accomplishments
- Created centralized `bridge.ts` module with `isTauri()`, `safeInvoke()`, `safeListen()`, `safeChannel()` using dynamic imports
- Removed all 5 top-level `@tauri-apps/api/*` imports across the app
- Web mode starts without `window.__TAURI_INTERNALS__` crashes
- Tauri desktop mode fully preserved with identical behavior
- `clearAuthStore` now falls back to `localStorage` in web mode instead of crashing on `@tauri-apps/plugin-store`

## Task Commits

1. **Task 1: Create bridge.ts** - `12f2e2a` (feat)
2. **Task 2: Fix +layout.svelte** - `e976030` (fix)
3. **Task 3: Fix auth.svelte.ts** - `5c161dd` (fix)
4. **Task 4: Fix auth.ts** - `d68eac5` (fix)
5. **Task 5: Fix agent.ts** - `c459ce5` (fix)

## Files Created/Modified
- `apps/client/web/app/src/lib/ipc/bridge.ts` — Centralized Tauri runtime abstraction (isTauri, safeInvoke, safeListen, safeChannel)
- `apps/client/web/app/src/routes/+layout.svelte` — Removed top-level Tauri imports, guarded all Tauri listeners with isTauri()
- `apps/client/web/app/src/lib/stores/auth.svelte.ts` — Replaced listen() with safeListen() for auth:expired event
- `apps/client/web/app/src/lib/ipc/auth.ts` — Replaced invoke() with safeInvoke(), clearAuthStore has Tauri/web fallback
- `apps/client/web/app/src/lib/ipc/agent.ts` — Replaced Channel/invoke with safeChannel()/safeInvoke()

## Decisions Made
- Dynamic import pattern (`await import(...)`) preferred over conditional require — matches project ESM module type
- `clearAuthStore` uses `localStorage` fallback in web mode for cross-runtime compatibility
- `safeChannel` returns `null` in web mode — callers check before use

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- None — all tasks completed cleanly
- svelte-check: 0 errors, 0 warnings
- cargo check: 0 errors (1 pre-existing serde warning)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Web mode startup is now crash-free — ready for browser-based E2E testing
- All Tauri IPC code paths have explicit dual-runtime support
- Bridge pattern established — future IPC integrations must use bridge.ts

---
*Phase: 10.3-uat-e2e*
*Completed: 2026-04-09*
