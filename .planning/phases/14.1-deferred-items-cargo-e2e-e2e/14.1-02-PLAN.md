---
phase: 14.1-deferred-items-cargo-e2e-e2e
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - moon.yml
  - Justfile
  - scripts/e2e/runtime-preflight.ts
  - apps/client/web/app/playwright.config.ts
  - apps/client/web/app/package.json
autonomous: true
requirements: [QGATE-01, QGATE-02]
must_haves:
  truths:
    - "Full E2E gate no longer spends unnecessary time on repeated runtime cold starts"
    - "E2E preflight catches type/port/runtime readiness issues before running suites"
    - "Repository gate still evaluates only active lanes: web + tauri"
  artifacts:
    - path: "scripts/e2e/runtime-preflight.ts"
      provides: "deterministic preflight for runtime health, port hygiene, and required artifacts"
    - path: "moon.yml"
      provides: "test-e2e-full wired to preflight + dual-lane deterministic summary"
    - path: "Justfile"
      provides: "human/agent entrypoint using updated full gate path"
  key_links:
    - from: "Justfile"
      to: "moon.yml"
      via: "test-e2e-full"
      pattern: "test-e2e-full"
    - from: "moon.yml"
      to: "scripts/e2e/runtime-preflight.ts"
      via: "preflight before lanes"
      pattern: "runtime-preflight"
---

<objective>
降低 full E2E 运行成本并消除常见假红，通过统一 preflight + runtime 复用策略稳住门禁入口。

Purpose: 解决 deferred-items 中“编译慢 + 启动摩擦”问题，让迭代速度和结果可靠性同时提升。  
Output: 可复用 preflight 脚本、已接线 full-gate 编排、更新的入口命令。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-CONTEXT.md
@.planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-RESEARCH.md
@moon.yml
@Justfile
@apps/client/web/app/playwright.config.ts
@apps/client/web/app/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增仓库级 runtime preflight 脚本并覆盖关键检查（D-03, D-04）</name>
  <files>scripts/e2e/runtime-preflight.ts</files>
  <read_first>
    - moon.yml
    - scripts/dev-desktop.mjs
    - e2e-desktop-playwright/tests/specs/tenant-isolation.spec.ts
    - apps/client/web/app/playwright.config.ts
  </read_first>
  <action>创建 `scripts/e2e/runtime-preflight.ts`，在 full gate 前执行：1) API `/readyz` 检查与按需启动建议；2) 端口冲突检测（5173/3001）；3) web 所需类型产物存在性检查并给出同步建议；4) 明确非 0 退出与诊断信息。该脚本只做测试编排准备，不改产品行为（per D-06）。</action>
  <acceptance_criteria>
    - 脚本包含 readyz 探测与超时机制。
    - 脚本包含 5173/3001 端口占用检测。
    - 脚本对缺失 `.svelte-kit/types` 给出明确失败信息或自动同步命令。
  </acceptance_criteria>
  <verify>
    <automated>rtk node scripts/e2e/runtime-preflight.ts</automated>
  </verify>
  <done>preflight 可独立运行并给出 deterministic 成功/失败信号。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 接线 moon/Just full gate 到 preflight + 双通道门禁（D-03, D-05）</name>
  <files>moon.yml, Justfile</files>
  <read_first>
    - moon.yml
    - Justfile
    - .github/workflows/e2e-tests.yml
  </read_first>
  <action>更新 `moon.yml` 的 `test-e2e-full` 执行顺序为“runtime-preflight -> web lane -> tauri lane summary”，确保仍是 web+tauri 双通道（per D-05），并在 `Justfile` 对应入口文案与命令保持一致。若某 lane 失败，summary 必须保留明确 lane 名称和 exit code。</action>
  <acceptance_criteria>
    - `moon.yml` 中 `test-e2e-full` 在 lane 执行前调用 preflight。
    - `Justfile` `test-e2e-full` 仍指向 `moon run repo:test-e2e-full`。
    - `test-e2e-full` 输出保留 lane 粒度 PASS/FAIL 汇总。
  </acceptance_criteria>
  <verify>
    <automated>rtk just test-e2e-full</automated>
  </verify>
  <done>全仓门禁入口具备预检、稳定编排与可诊断摘要。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 优化 web Playwright 启动策略与脚本入口一致性（D-03, D-04）</name>
  <files>apps/client/web/app/playwright.config.ts, apps/client/web/app/package.json</files>
  <read_first>
    - apps/client/web/app/playwright.config.ts
    - apps/client/web/app/package.json
    - apps/client/web/app/tests/fixtures/runtime.ts
  </read_first>
  <action>在 web Playwright 配置中补充（或接线）`globalSetup` 到 runtime/type preflight 能力，并保持 `webServer.reuseExistingServer` 与 CI 语义不变；在 package scripts 中增加/更新可复用入口（如 `test:e2e:preflight`），让本地与 CI 均可复现同一启动策略。</action>
  <acceptance_criteria>
    - `playwright.config.ts` 存在 `globalSetup` 或等效统一预检接线。
    - `package.json` 存在与 preflight 一致的可复用脚本入口。
    - 执行 web E2E 时不再出现“缺类型文件后才失败”的晚期错误。
  </acceptance_criteria>
  <verify>
    <automated>rtk bun run --cwd apps/client/web/app test:e2e</automated>
  </verify>
  <done>web E2E 启动路径与仓库 preflight 策略统一，减少启动随机性。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preflight script → host processes/ports | 预检需要读取端口与进程状态，若误判会造成假红或误放行 |
| full gate orchestrator → release signal | 编排错误会影响门禁可信度 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-04 | D | runtime preflight | mitigate | 统一检查逻辑并输出确定性错误码，避免随机重试驱动结果 |
| T-14.1-05 | T | moon full-gate orchestration | mitigate | 强制 preflight 在前，保留 lane 级 exit code 与 summary |
| T-14.1-06 | R | CLI entrypoint parity | mitigate | Just 与 moon 保持单一入口，减少“命令路径不同”导致的不可复现 |
</threat_model>

<verification>
- `rtk node scripts/e2e/runtime-preflight.ts`
- `rtk bun run --cwd apps/client/web/app test:e2e`
- `rtk just test-e2e-full`
</verification>

<success_criteria>
- full gate 前置 deterministic preflight。
- web/tauri 双通道门禁编排保持一致且可诊断。
- E2E 启动随机性明显降低（端口/类型/ready 问题前移暴露）。
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-02-SUMMARY.md`
</output>
