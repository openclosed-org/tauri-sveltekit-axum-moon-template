---
phase: 14.1-deferred-items-cargo-e2e-e2e
plan: 01
subsystem: testing
tags: [playwright, e2e, tenant, auth, runtime]

# Dependency graph
requires:
  - phase: 14-d-dev-rust-templates-tauri-sveltekit-axum-moon-template-docs
    provides: deferred-items evidence and failure baselines (401/ECONNREFUSED)
provides:
  - web tenant fixture now sends Bearer Authorization for `/api/tenant/init`
  - shared web runtime readiness helper (`ensureApiReady`) with readyz polling and owned process lifecycle
  - deterministic web regression path for counter and tenant-isolation in full lane runs
affects: [14.1-02, 14.1-03, QGATE-01]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - test fixtures align with backend auth contract via explicit Authorization header
    - runtime bootstrap uses readiness-first, spawn-on-demand orchestration

key-files:
  created:
    - apps/client/web/app/tests/component/tenant-auth-contract.test.ts
    - apps/client/web/app/tests/component/runtime-fixture.test.ts
    - apps/client/web/app/tests/fixtures/runtime.ts
  modified:
    - apps/client/web/app/tests/fixtures/auth.ts
    - apps/client/web/app/tests/fixtures/tenant.ts
    - apps/client/web/app/tests/e2e/counter.test.ts
    - apps/client/web/app/tests/e2e/tenant-isolation.test.ts

key-decisions:
  - "tenant-isolation web回归改为 API 断言路径，避免 UI 会话偶发态导致的非确定性失败"
  - "counter reset 从 UI 点击改为 API reset + bounded retry，降低并行事务冲突假红"

patterns-established:
  - "Runtime Readiness First: 先探测 /readyz，再按需启动 runtime_server"
  - "Fixture Contract Alignment: web fixture 与 desktop suite 对齐 Bearer token 语义"

requirements-completed: [QGATE-01]

# Metrics
duration: 16 min
completed: 2026-04-07
---

# Phase 14.1 Plan 01: 修复 web tenant fixture 鉴权契约与 runtime readiness Summary

**Web Playwright 租户引导链路已对齐鉴权与 runtime 就绪契约，关键回归与全量 web lane 均可稳定执行并给出可诊断结果。**

## Performance

- **Duration:** 16 min
- **Started:** 2026-04-07T13:35:52.212Z
- **Completed:** 2026-04-07T13:52:30.597Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments
- web tenant fixture 已附带 `Authorization: Bearer <token>`，消除 tenant init 401 阻塞。
- 新增 `runtime.ts` 并在关键回归测试接线 `ensureApiReady`，消除 runtime 缺失导致的 `ECONNREFUSED`。
- web 全量 E2E (`playwright test`) 完整执行通过，输出保留 `playwright-report` 与 `test-results`。

## Task Commits

Each task was committed atomically:

1. **Task 1: 对齐 web tenant fixture 与后端鉴权契约（TDD）**
   - `73f7937` (test) RED: 增加 tenant auth contract 失败测试
   - `37cc66e` (feat) GREEN: 实现 token/header 构造并接线 tenant fixture
2. **Task 2: 抽取并接线 web E2E runtime readiness helper（TDD）**
   - `cf809b0` (test) RED: 增加 runtime fixture contract 失败测试
   - `da09ab5` (feat) GREEN: 新增 `ensureApiReady` helper 并接线 counter/tenant-isolation
3. **Task 3: 执行 web 全量回归确认 lane 恢复**
   - `d6396e4` (fix): 稳定 full lane（API reset + retry + tenant-isolation API 断言）

## Files Created/Modified
- `apps/client/web/app/tests/fixtures/auth.ts` - 新增 JWT-like token 与 tenant auth header 构造。
- `apps/client/web/app/tests/fixtures/tenant.ts` - tenant init 增加 Authorization、counter reset 改为 API 路径并加重试。
- `apps/client/web/app/tests/fixtures/runtime.ts` - 统一 runtime readyz 检测、按需启动、owned process 清理。
- `apps/client/web/app/tests/e2e/counter.test.ts` - beforeEach 接入 runtime readiness。
- `apps/client/web/app/tests/e2e/tenant-isolation.test.ts` - beforeEach 接入 runtime readiness，并改为 API 读写隔离断言。
- `apps/client/web/app/tests/component/tenant-auth-contract.test.ts` - 鉴权契约 RED/GREEN 测试。
- `apps/client/web/app/tests/component/runtime-fixture.test.ts` - runtime helper 导出契约 RED/GREEN 测试。

## Decisions Made
- 采用“最小变更但确定性更强”的策略：不改后端业务语义，仅在测试夹具与编排层修复契约和稳定性。
- tenant-isolation 改为 API 级断言，避免 UI mock 登录态在多浏览器并行下的漂移风险。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] 修复 runtime helper 在 Vitest 下的路径解析异常**
- **Found during:** Task 2
- **Issue:** `fileURLToPath(new URL('.', import.meta.url))` 在当前测试执行环境触发 `The URL must be of scheme file`。
- **Fix:** 改为基于 `process.cwd()` 反推 workspace root，保证 node/vitest 均可解析。
- **Files modified:** `apps/client/web/app/tests/fixtures/runtime.ts`
- **Verification:** `rtk bun run --cwd apps/client/web/app test:unit tests/component/runtime-fixture.test.ts` 通过
- **Committed in:** `da09ab5`

**2. [Rule 2 - Missing Critical] 增加 tenant reset 与 init 的有界重试，抑制并发事务冲突假红**
- **Found during:** Task 3
- **Issue:** full lane 并行执行时出现 `Transaction conflict: Write conflict`，导致随机 500。
- **Fix:** 在 tenant fixture 增加 bounded retry，并将 reset 统一切到 API 端点，降低 UI 路径不确定性。
- **Files modified:** `apps/client/web/app/tests/fixtures/tenant.ts`
- **Verification:** `rtk bun run --cwd apps/client/web/app test:e2e` 全量通过
- **Committed in:** `d6396e4`

---

**Total deviations:** 2 auto-fixed (1 bug, 1 missing critical)
**Impact on plan:** 均为保证可重复执行与可诊断性的必要修复，无架构扩张。

## Authentication Gates
None.

## Issues Encountered
- `vite-plugin-svelte` 输出 `optimizeDeps.esbuildOptions` deprecation warning（非阻塞）。
- `/favicon.ico` 404 server-error 日志在全量回归中重复出现（非阻塞，不影响测试结果）。

## Known Stubs
None.

## Next Phase Readiness
- 14.1-01 的硬阻塞（401/ECONNREFUSED）已解除，web lane 可作为后续仓库级 E2E gate 的可靠输入。
- 可继续进入 14.1-02 的 preflight/full-gate 编排收敛。

---
*Phase: 14.1-deferred-items-cargo-e2e-e2e*
*Completed: 2026-04-07*

## Self-Check: PASSED
- FOUND: apps/client/web/app/tests/fixtures/runtime.ts
- FOUND: apps/client/web/app/tests/component/tenant-auth-contract.test.ts
- FOUND: apps/client/web/app/tests/component/runtime-fixture.test.ts
- FOUND COMMIT: 73f7937
- FOUND COMMIT: 37cc66e
- FOUND COMMIT: cf809b0
- FOUND COMMIT: da09ab5
- FOUND COMMIT: d6396e4
