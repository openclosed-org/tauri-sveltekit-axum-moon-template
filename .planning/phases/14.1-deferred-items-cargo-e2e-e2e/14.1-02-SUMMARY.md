---
phase: 14.1-deferred-items-cargo-e2e-e2e
plan: 02
subsystem: testing
tags: [e2e, playwright, moon, just, preflight, runtime]

requires:
  - phase: 14.1-01
    provides: web tenant fixture 鉴权契约与 runtime readiness 基线
provides:
  - 仓库级 runtime preflight（readyz/端口/类型产物）
  - full gate 前置 preflight 与 lane 级 exit code 汇总
  - web Playwright globalSetup 与 preflight 入口脚本一致化
affects: [Phase 14.1-03, CI E2E gate, local e2e diagnostics]

tech-stack:
  added: []
  patterns: [deterministic preflight before lane execution, fail-fast diagnostics, shared startup contract]

key-files:
  created:
    - scripts/e2e/runtime-preflight.ts
    - scripts/e2e/runtime-preflight.test.mjs
    - apps/client/web/app/tests/fixtures/global-setup.ts
    - .planning/phases/14.1-deferred-items-cargo-e2e-e2e/deferred-items.md
  modified:
    - moon.yml
    - Justfile
    - apps/client/web/app/playwright.config.ts
    - apps/client/web/app/package.json
    - apps/client/web/app/tests/fixtures/runtime.ts

key-decisions:
  - "仓库 full gate 先跑 runtime preflight，失败即 fail-fast 并输出可诊断错误"
  - "web E2E 通过 Playwright globalSetup 复用 runtime/type 预检能力，保持 CI reuse 语义不变"

patterns-established:
  - "Pattern: repo gate preflight-first（先健康检查再跑 lane）"
  - "Pattern: web startup parity（globalSetup + package script 使用同一 preflight 能力）"

requirements-completed: [QGATE-01, QGATE-02]

duration: 11 min
completed: 2026-04-07
---

# Phase 14.1 Plan 02: 新增仓库级 runtime preflight 并接线 full E2E gate Summary

**交付了可复用的 runtime preflight 与 full-gate 前置编排，使端口/readyz/类型产物问题在跑套件前被确定性拦截并给出 lane 级诊断。**

## Performance

- **Duration:** 11 min
- **Started:** 2026-04-07T22:02:55+08:00
- **Completed:** 2026-04-07T22:14:01+08:00
- **Tasks:** 3/3
- **Files modified:** 9

## Accomplishments
- 新增 `scripts/e2e/runtime-preflight.ts`，覆盖 `/readyz` 超时探测、5173/3001 端口卫生与 `.svelte-kit/types` 产物检查。
- `moon.yml` 的 `test-e2e-full` 已改为 preflight-first，且保留 web/tauri lane 的 PASS/FAIL 与 exit code 汇总。
- web Playwright 增加 `globalSetup`，并在 `package.json` 增加 `test:e2e:preflight` 入口，保证本地与 CI 启动策略一致。

## Task Commits

1. **Task 1 (RED): 新增 preflight 失败用例** - `117a293` (test)
2. **Task 1 (GREEN): 实现 runtime preflight** - `e5fea61` (feat)
3. **Task 2: 接线 moon/Just full gate 到 preflight + 双通道摘要** - `4805feb` (feat)
4. **Task 3: 对齐 web Playwright 启动策略与脚本入口** - `27b63af` (feat)

## Files Created/Modified
- `scripts/e2e/runtime-preflight.ts` - 仓库级 preflight 脚本，确定性返回成功/失败。
- `scripts/e2e/runtime-preflight.test.mjs` - preflight 行为测试（readyz/types/ports/pass）。
- `moon.yml` - `test-e2e-full` 前置 preflight，失败时立即输出 gate summary。
- `Justfile` - `test-e2e-full` 描述更新为 preflight + web + tauri。
- `apps/client/web/app/playwright.config.ts` - 接线 `globalSetup`。
- `apps/client/web/app/tests/fixtures/global-setup.ts` - 启动前执行 runtime/type preflight。
- `apps/client/web/app/tests/fixtures/runtime.ts` - 增加 `ensureWebE2EPreflight`，统一 API + 类型产物检查。
- `apps/client/web/app/package.json` - 新增 `test:e2e:preflight` 可复用入口。
- `.planning/phases/14.1-deferred-items-cargo-e2e-e2e/deferred-items.md` - 记录本计划范围外阻塞项。

## Decisions Made
- 继续保持活动 lane 仅 `web + tauri`，不引入额外门禁通道（遵循 D-05）。
- preflight 不改业务逻辑，只做测试编排与运行前健康检查（遵循 D-06）。

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `rtk bun run --cwd apps/client/web/app test:e2e` 在 Safari lane 存在既有失败（`agent`/`counter` 选择器元素缺失），不属于本计划 preflight/编排改动范围，已记录到 `deferred-items.md`。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `test-e2e-full` 现已具备 preflight fail-fast 与 lane 级可诊断摘要，适合 Phase 14.1-03 继续收口 CI/验证契约。
- 仍需在后续计划处理 Safari lane 的历史不稳定用例，才能恢复全矩阵全绿。

## Known Stubs

None.

## Self-Check: PASSED

- FOUND summary file: `.planning/phases/14.1-deferred-items-cargo-e2e-e2e/14.1-02-SUMMARY.md`
- FOUND commit: `117a293`
- FOUND commit: `e5fea61`
- FOUND commit: `4805feb`
- FOUND commit: `27b63af`

---
*Phase: 14.1-deferred-items-cargo-e2e-e2e*
*Completed: 2026-04-07*
