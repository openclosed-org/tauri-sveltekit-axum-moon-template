---
phase: 09-functional-correctness-baseline-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/web/app/src/routes/(app)/counter/+page.svelte
  - apps/client/web/app/tests/component/counter.test.ts
  - apps/client/web/app/tests/e2e/counter.test.ts
autonomous: true
requirements: [COUNTER-02]
must_haves:
  truths:
    - "User can increment and decrement counter with correct visible value"
    - "Counter errors are visible and do not silently corrupt displayed state"
    - "After reload, displayed value matches persisted value"
  artifacts:
    - path: "apps/client/web/app/src/routes/(app)/counter/+page.svelte"
      provides: "Counter command handling + error banner + consistency logic"
    - path: "apps/client/web/app/tests/component/counter.test.ts"
      provides: "UI logic regression coverage"
    - path: "apps/client/web/app/tests/e2e/counter.test.ts"
      provides: "reload persistence assertion"
  key_links:
    - from: "counter/+page.svelte"
      to: "counter commands"
      via: "invokeCommand + response value assignment"
      pattern: "count = await invokeCommand"
---

<objective>
Enforce counter correctness baseline with visible error handling and persisted-value consistency per D-05~D-08.

Purpose: Remove silent counter failures that would invalidate later regression gates.
Output: Counter page behavior fix + component/e2e assertions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-functional-correctness-baseline-fix/09-CONTEXT.md
@.planning/phases/09-functional-correctness-baseline-fix/09-RESEARCH.md

<interfaces>
From `packages/adapters/hosts/tauri/src/commands/counter.rs`:
```rs
pub async fn counter_increment(app: AppHandle) -> Result<i64, String>;
pub async fn counter_decrement(app: AppHandle) -> Result<i64, String>;
pub async fn counter_reset(app: AppHandle) -> Result<i64, String>;
pub async fn counter_get_value(app: AppHandle) -> Result<i64, String>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor counter page for explicit error and source-of-truth update</name>
  <files>apps/client/web/app/src/routes/(app)/counter/+page.svelte, apps/client/web/app/tests/component/counter.test.ts</files>
  <read_first>
    - apps/client/web/app/src/routes/(app)/counter/+page.svelte
    - apps/client/web/app/tests/component/counter.test.ts
    - .planning/phases/09-functional-correctness-baseline-fix/09-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: loadValue failure keeps count at 0 and shows error banner (per D-07).
    - Test 2: increment/decrement/reset success updates count from returned value only (per D-06).
    - Test 3: mutate failure preserves previous successful count and shows visible error (per D-05).
  </behavior>
  <action>Introduce `errorMessage` reactive state on counter page. Replace empty catch blocks with typed error handling. On mount, call `loadValue()`; if request fails, set `count=0` and `errorMessage='Failed to load persisted counter value'`. For increment/decrement/reset, wrap in try/catch; on success assign exactly returned value, clear error; on failure keep existing count and set operation-specific banner text. Keep existing command names and dual-path runtime behavior unchanged.
</action>
  <acceptance_criteria>
    - `counter/+page.svelte` contains `let errorMessage = $state(`.
    - catch blocks set `errorMessage` instead of being empty.
    - load failure path contains explicit `count = 0`.
    - template includes visible error banner block bound to `errorMessage`.
    - component tests include failure-case assertions for load and mutation.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/client/web/app test:unit -- tests/component/counter.test.ts</automated>
  </verify>
  <done>Counter page has deterministic success/error behavior and no silent failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reload-persistence E2E assertion for counter baseline</name>
  <files>apps/client/web/app/tests/e2e/counter.test.ts</files>
  <read_first>
    - apps/client/web/app/tests/e2e/counter.test.ts
    - apps/client/web/app/src/routes/(app)/counter/+page.svelte
    - .planning/ROADMAP.md
  </read_first>
  <behavior>
    - Test 1: after increment/decrement operations and page reload, displayed count equals persisted backend value (per D-08).
  </behavior>
  <action>Add E2E test case named `persists counter value after reload` in `counter.test.ts`. Flow: authenticate, visit `/counter`, click increment twice and decrement once, capture displayed value, call `page.reload({ waitUntil: 'networkidle' })`, assert counter display still equals captured value. Keep guard-friendly pattern used by existing suite when redirected to login.
</action>
  <acceptance_criteria>
    - E2E file contains test title matching `persists counter value after reload`.
    - test includes `page.reload` and post-reload counter assertion.
    - command `test:e2e --grep "counter"` includes the new case.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/client/web/app test:e2e --grep "counter"</automated>
  </verify>
  <done>Counter persistence baseline is regression-tested in browser flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → counter command/API | Untrusted operation requests mutate persisted state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | T | Counter mutation flow | mitigate | Use backend return value as single source of truth; never optimistic overwrite |
| T-09-06 | R | Counter failure visibility | mitigate | Render explicit error banner so failed operations are auditable by user/test |
| T-09-07 | D | Counter endpoint | accept | Local app scope and low-cost operations; covered by existing auth guard and retries |
</threat_model>

<verification>
- `bun run --cwd apps/client/web/app test:unit -- tests/component/counter.test.ts`
- `bun run --cwd apps/client/web/app test:e2e --grep "counter"`
</verification>

<success_criteria>
- COUNTER-02 is met for success path, failure path, and reload persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/09-functional-correctness-baseline-fix/09-02-SUMMARY.md`
</output>
