---
phase: 09-functional-correctness-baseline-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/web/app/src/routes/(app)/settings/+page.svelte
  - apps/client/web/app/src/lib/stores/auth.svelte.ts
  - apps/client/web/app/src/lib/ipc/auth.ts
  - packages/adapters/hosts/tauri/src/commands/auth.rs
  - apps/client/native/src-tauri/src/lib.rs
  - apps/client/web/app/tests/component/auth.test.ts
  - apps/client/web/app/tests/component/settings-connection.test.ts
autonomous: true
requirements: [AUTH-02, AUTH-03, AGENT-04]
must_haves:
  truths:
    - "Signed-in user can see and click logout in Settings"
    - "Logout clears reusable session credentials and returns to unauthenticated flow"
    - "User can run Test Connection and get API key/Base URL/Model pass-fail with actionable guidance"
  artifacts:
    - path: "apps/client/web/app/src/routes/(app)/settings/+page.svelte"
      provides: "Logout and Test Connection UI + result rendering"
    - path: "apps/client/web/app/src/lib/stores/auth.svelte.ts"
      provides: "Dual-path logout orchestration and fallback redirect"
    - path: "packages/adapters/hosts/tauri/src/commands/auth.rs"
      provides: "Tauri logout command for remote/session invalidation"
  key_links:
    - from: "settings/+page.svelte"
      to: "auth.svelte.ts signOut"
      via: "Logout button onclick"
      pattern: "onclick=\{.*signOut.*\}"
    - from: "auth.svelte.ts"
      to: "ipc/auth.ts"
      via: "logout invoke then clearAuthStore fallback"
      pattern: "await logout\(\).*await clearAuthStore\(\)"
---

<objective>
Deliver Settings-based logout and connectivity diagnostics with deterministic session clearing and actionable feedback, matching D-01~D-04 and D-12~D-14.

Purpose: Establish a trustworthy auth/config baseline before regression gate phases.
Output: Logout command wiring + Settings actions + auth/config verification tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-functional-correctness-baseline-fix/09-CONTEXT.md
@.planning/phases/09-functional-correctness-baseline-fix/09-RESEARCH.md

<interfaces>
From `apps/client/web/app/src/lib/ipc/auth.ts`:
```ts
export async function startOAuth(): Promise<void>;
export async function handleOAuthCallback(url: string): Promise<AuthSession>;
export async function getSession(): Promise<AuthSession | null>;
export async function clearAuthStore(): Promise<void>;
```

From `packages/adapters/hosts/tauri/src/commands/auth.rs`:
```rs
#[tauri::command]
pub async fn start_oauth(app: AppHandle) -> Result<(), String>;
#[tauri::command]
pub async fn handle_oauth_callback(app: AppHandle, url: String) -> Result<AuthSession, String>;
#[tauri::command]
pub async fn get_session(app: AppHandle) -> Result<Option<AuthSession>, String>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit logout command and frontend dual-path sign-out</name>
  <files>packages/adapters/hosts/tauri/src/commands/auth.rs, apps/client/native/src-tauri/src/lib.rs, apps/client/web/app/src/lib/ipc/auth.ts, apps/client/web/app/src/lib/stores/auth.svelte.ts, apps/client/web/app/tests/component/auth.test.ts</files>
  <read_first>
    - apps/client/web/app/src/lib/stores/auth.svelte.ts
    - apps/client/web/app/src/lib/ipc/auth.ts
    - packages/adapters/hosts/tauri/src/commands/auth.rs
    - apps/client/native/src-tauri/src/lib.rs
    - .planning/phases/09-functional-correctness-baseline-fix/09-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: signOut calls remote logout first; if remote fails, local clearAuthStore still executes (per D-02/D-03).
    - Test 2: signOut resets auth state and navigates to unauthenticated route (per D-04).
  </behavior>
  <action>Per D-02/D-03, add `#[tauri::command] pub async fn logout(app: AppHandle) -> Result<(), String>` in `auth.rs` calling `TauriAuthService::logout()`. Register `auth::logout` in `src-tauri/lib.rs` invoke handler. In `ipc/auth.ts`, add `logout(): Promise<void>` with `invoke('logout')`. In `auth.svelte.ts`, update `signOut()` to execute `logout()` in try/catch, always call `clearAuthStore()` in finally-style flow, then reset `isAuthenticated/currentUser/tokenExpiresAt/authError` and navigate to public target (`/login` fallback) per D-04. Update unit tests to assert call ordering and fallback behavior.</action>
  <acceptance_criteria>
    - `packages/adapters/hosts/tauri/src/commands/auth.rs` contains `pub async fn logout(` and returns `Result<(), String>`.
    - `apps/client/native/src-tauri/src/lib.rs` invoke list contains `auth::logout`.
    - `apps/client/web/app/src/lib/ipc/auth.ts` contains `invoke('logout')`.
    - `apps/client/web/app/src/lib/stores/auth.svelte.ts` signOut includes both `await logout()` and `await clearAuthStore()` with fallback path.
    - `auth.test.ts` includes a case asserting local clear executes when remote logout throws.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/client/web/app test:unit -- tests/component/auth.test.ts</automated>
  </verify>
  <done>Logout flow is callable from frontend, invalidates remote/local session state, and is covered by component tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Settings logout + Test Connection tri-state diagnostics</name>
  <files>apps/client/web/app/src/routes/(app)/settings/+page.svelte, apps/client/web/app/tests/component/settings-connection.test.ts</files>
  <read_first>
    - apps/client/web/app/src/routes/(app)/settings/+page.svelte
    - apps/client/web/app/src/routes/(app)/agent/+page.svelte
    - packages/core/usecases/src/agent_service.rs
    - .planning/phases/09-functional-correctness-baseline-fix/09-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Settings shows visible Logout action in page body (per D-01).
    - Test 2: Test Connection returns/render three separate rows (API key/Base URL/Model) each with pass/fail and suggestion (per D-13).
    - Test 3: On failed test, form values remain unchanged (per D-14).
  </behavior>
  <action>In settings page add secondary `Logout` button that calls `signOut()` (per D-01). Add `Test Connection` button adjacent to save action (per D-12). Implement connection test function: (1) API key check = non-empty and prefix hint; (2) Base URL check = valid URL parse + fetch to `${baseUrl}/models` with timeout; (3) Model check = response model list contains selected model or returns explicit not-found. Render structured result cards with `status: pass/fail` and `next_step` text for each dimension (per D-13). Do not mutate `apiKey/baseUrl/model` in failure paths (per D-14). Add unit tests covering pass/fail permutations and value persistence.
</action>
  <acceptance_criteria>
    - `settings/+page.svelte` contains visible text `Logout` and `Test Connection`.
    - settings component contains three result labels exactly: `API key`, `Base URL`, `Model`.
    - each result rendering includes `pass` or `fail` plus recommendation text node.
    - failed connection test does not call any setter that rewrites `apiKey/baseUrl/model`.
    - `tests/component/settings-connection.test.ts` exists and has cases for all three dimensions.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/client/web/app test:unit -- tests/component/settings-connection.test.ts</automated>
  </verify>
  <done>Settings provides required logout and connectivity diagnostics with deterministic, non-destructive retry UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web UI → Tauri command | Untrusted UI action requests logout |
| Web UI → External LLM endpoint | User-provided API base/model values cross network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | S | Logout action | mitigate | Require explicit command invocation + reset local auth state regardless of remote response |
| T-09-02 | T | Settings connection test | mitigate | Validate URL format and sanitize displayed error details (no secret echo) |
| T-09-03 | I | API key handling | mitigate | Never render full API key; diagnostics show only status/recommendation |
| T-09-04 | D | Connection test endpoint | accept | Single-user local tool; timeout and cancel budget limits resource impact |
</threat_model>

<verification>
- `bun run --cwd apps/client/web/app test:unit -- tests/component/auth.test.ts`
- `bun run --cwd apps/client/web/app test:unit -- tests/component/settings-connection.test.ts`
</verification>

<success_criteria>
- AUTH-02/AUTH-03/AGENT-04 behaviors are covered by executable tests.
- Settings provides logout + tri-state connection diagnostics without destructive rollback.
</success_criteria>

<output>
After completion, create `.planning/phases/09-functional-correctness-baseline-fix/09-01-SUMMARY.md`
</output>
