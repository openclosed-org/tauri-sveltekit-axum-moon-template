---
phase: 09-functional-correctness-baseline-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/web/app/src/routes/(app)/agent/+page.svelte
  - apps/client/web/app/tests/e2e/agent.test.ts
  - apps/client/web/app/tests/component/agent-phase9.test.ts
autonomous: true
requirements: [AGENT-02, AGENT-03]
must_haves:
  truths:
    - "New Chat immediately starts a fresh conversation thread"
    - "New Chat keeps saved API key, base URL, and model settings intact"
    - "Agent page shows actionable settings-read failure guidance when settings cannot be loaded"
  artifacts:
    - path: "apps/client/web/app/src/routes/(app)/agent/+page.svelte"
      provides: "Conversation reset semantics + settings fallback guidance"
    - path: "apps/client/web/app/tests/component/agent-phase9.test.ts"
      provides: "New Chat and settings retention unit coverage"
    - path: "apps/client/web/app/tests/e2e/agent.test.ts"
      provides: "Browser-level fresh-thread verification"
  key_links:
    - from: "agent/+page.svelte"
      to: "settings.json store"
      via: "loadSettings fallback/defaults"
      pattern: "Store.load\('settings.json'\)"
---

<objective>
Lock agent thread reset behavior so New Chat creates a clean conversation without losing persisted configuration per D-09~D-11.

Purpose: Prevent settings regressions while fixing conversation semantics.
Output: Agent page behavior fix + fresh-thread regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-functional-correctness-baseline-fix/09-CONTEXT.md
@.planning/phases/09-functional-correctness-baseline-fix/09-RESEARCH.md

<interfaces>
From `apps/client/web/app/src/lib/ipc/agent.ts`:
```ts
export type Conversation = { id: string; title: string; created_at?: string };
export async function listConversations(): Promise<Conversation[]>;
export async function createConversation(title: string): Promise<Conversation>;
export async function getConversationMessages(id: string): Promise<ChatMessage[]>;
export async function agentChatStream(params: {...}): AsyncGenerator<string, void, unknown>;
```

From `apps/client/web/app/src/routes/(app)/agent/+page.svelte`:
```ts
let conversations = $state<Conversation[]>([]);
let activeConversation = $state<string | null>(null);
let messages = $state<ChatMessage[]>([]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make New Chat reset thread state while preserving settings</name>
  <files>apps/client/web/app/src/routes/(app)/agent/+page.svelte, apps/client/web/app/tests/component/agent-phase9.test.ts</files>
  <read_first>
    - apps/client/web/app/src/routes/(app)/agent/+page.svelte
    - apps/client/web/app/tests/component/agent-phase9.test.ts
    - apps/client/web/app/src/lib/ipc/agent.ts
    - .planning/phases/09-functional-correctness-baseline-fix/09-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: clicking New Chat immediately sets activeConversation to the new id and clears message list (per D-09).
    - Test 2: settings loader retains api_key/base_url/model after New Chat (per D-10).
    - Test 3: settings read failure shows actionable guidance but continues with defaults (per D-11).
  </behavior>
  <action>Adjust `createConversation()` so it sets `activeConversation = conv.id`, resets `messages = []`, and focuses the input-ready state immediately after creation instead of waiting for a reload cycle. Keep `loadSettings()` returning defaults on error, but replace silent catch with a visible `loadError`/guidance message explaining that `settings.json` could not be read and the user should open Settings to resave configuration. Do not reset or mutate `api_key/base_url/model` when creating a new chat; only thread-local state is cleared.
</action>
  <acceptance_criteria>
    - `agent/+page.svelte` contains `activeConversation = conv.id` inside `createConversation()`.
    - `agent/+page.svelte` contains `messages = []` after New Chat creation.
    - settings read failure path contains user-facing guidance text and default return object.
    - component tests assert settings persist across new conversation creation.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/client/web/app test:unit -- tests/component/agent-phase9.test.ts</automated>
  </verify>
  <done>New Chat semantics are thread-clean and settings-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add browser E2E coverage for fresh-thread and config retention semantics</name>
  <files>apps/client/web/app/tests/e2e/agent.test.ts</files>
  <read_first>
    - apps/client/web/app/tests/e2e/agent.test.ts
    - apps/client/web/app/src/routes/(app)/agent/+page.svelte
    - .planning/ROADMAP.md
  </read_first>
  <behavior>
    - Test 1: after clicking New Chat, conversation list selects the new thread and message area is empty.
    - Test 2: settings text still reflects retained API key/base URL/model state when navigating back to settings.
  </behavior>
  <action>Add a new Playwright case that authenticates, enters Agent page, clicks New Chat, and asserts the message pane resets to empty state while the settings store remains unchanged after navigating to Settings and back. Reuse existing guard-tolerant pattern used in `agent.test.ts` when auth redirects occur.
</action>
  <acceptance_criteria>
    - `agent.test.ts` contains a case for New Chat clearing the thread.
    - test includes navigation to Settings and verification that saved config UI is unchanged.
    - `test:e2e --grep "agent"` covers the new case.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/client/web/app test:e2e --grep "agent"</automated>
  </verify>
  <done>Agent browser flow proves new-thread semantics and settings preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → settings store | Untrusted state transition must not clobber saved credentials |
| UI → conversation service | New thread creation mutates conversation state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | T | New Chat state transition | mitigate | Clear only thread-local state, not persisted settings |
| T-09-09 | I | Settings read failure | mitigate | Show actionable guidance without exposing secret values |
| T-09-10 | D | Conversation creation | accept | Local user-driven action; existing API guard and retries suffice |
</threat_model>

<verification>
- `bun run --cwd apps/client/web/app test:unit -- tests/component/agent-phase9.test.ts`
- `bun run --cwd apps/client/web/app test:e2e --grep "agent"`
</verification>

<success_criteria>
- AGENT-02/AGENT-03 behaviors are verified in both unit and browser flows.
</success_criteria>

<output>
After completion, create `.planning/phases/09-functional-correctness-baseline-fix/09-03-SUMMARY.md`
</output>
