---
phase: 04-minimal-feature-implementation
plan: '05'
type: execute
wave: 5
depends_on:
  - 04-01
  - 04-03
files_modified:
  - packages/adapters/hosts/tauri/Cargo.toml
  - packages/adapters/hosts/tauri/src/commands/auth.rs
  - packages/adapters/hosts/tauri/src/commands/mod.rs
  - apps/client/native/src-tauri/src/lib.rs
autonomous: true
gap_closure: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "Google 登录命令仍可用，但 runtime_tauri auth 只做 host 薄封装（不再内联 OAuth 细节）"
    - "runtime_tauri auth 通过 feature-auth::AuthService 边界调用 adapter-google（per D-01, D-04）"
    - "auth 功能路径满足 feature + adapter 约束（per D-01），不再绕过 feature-auth"
  artifacts:
    - path: "packages/adapters/hosts/tauri/src/commands/auth.rs"
      provides: "Tauri auth command thin wrapper + AuthService bridge"
      contains: "feature_auth::AuthService"
    - path: "packages/adapters/hosts/tauri/Cargo.toml"
      provides: "runtime_tauri auth wiring dependencies"
      contains: "feature-auth"
    - path: "packages/adapters/hosts/tauri/Cargo.toml"
      provides: "runtime_tauri adapter wiring dependencies"
      contains: "adapter-google"
  key_links:
    - from: "packages/adapters/hosts/tauri/src/commands/auth.rs"
      to: "packages/features/auth/src/lib.rs"
      via: "AuthService implementation/usage"
      pattern: "impl\\s+feature_auth::AuthService|feature_auth::AuthService"
    - from: "packages/adapters/hosts/tauri/src/commands/auth.rs"
      to: "packages/adapters/auth/google/src/lib.rs"
      via: "GoogleAuthAdapter delegation"
      pattern: "GoogleAuthAdapter"
---

<objective>
关闭 Phase 4 的 AUTH-01 gap：把 runtime_tauri auth 从“内联 OAuth 实现”改为“host 薄封装 + feature/auth adapter 接线”。

Purpose: 满足验证报告的 blocker（04-VERIFICATION.md），确保 auth 不污染 host 层并回到既定边界。
Output: runtime_tauri auth 命令仅做委托，adapter-google 与 feature-auth 形成可验证调用链。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-minimal-feature-implementation/04-CONTEXT.md
@.planning/phases/04-minimal-feature-implementation/04-VERIFICATION.md
@.planning/phases/04-minimal-feature-implementation/04-01-SUMMARY.md
@.planning/phases/04-minimal-feature-implementation/04-03-SUMMARY.md
@packages/adapters/hosts/tauri/src/commands/auth.rs
@packages/adapters/auth/google/src/lib.rs
@packages/features/auth/src/lib.rs

<interfaces>
From packages/features/auth/src/lib.rs:
```rust
pub trait AuthService: Send + Sync {
    async fn start_login(&self) -> Result<(), AuthError>;
    async fn handle_callback(&self, url: &str) -> Result<AuthResult, AuthError>;
    async fn get_session(&self) -> Result<Option<SessionInfo>, AuthError>;
    async fn logout(&self) -> Result<(), AuthError>;
}
```

From packages/adapters/auth/google/src/lib.rs:
```rust
pub struct GoogleAuthAdapter;
impl GoogleAuthAdapter {
    pub fn new() -> Self;
    pub async fn start_login(&self, app: &AppHandle) -> Result<(), AuthError>;
    pub async fn handle_callback(&self, app: &AppHandle, url: &str) -> Result<AuthSession, AuthError>;
    pub fn get_session(&self, app: &AppHandle) -> Result<Option<AuthSession>, String>;
    pub async fn refresh_token(&self, app: &AppHandle, refresh_token: &str) -> Result<(String, u64), String>;
    pub fn clear_session(&self, app: &AppHandle);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 runtime_tauri 建立 AuthService→GoogleAuthAdapter 接线（per D-04）</name>
  <files>packages/adapters/hosts/tauri/Cargo.toml, packages/adapters/hosts/tauri/src/commands/auth.rs</files>
  <action>在 runtime_tauri 引入 `feature-auth` 与 `adapter-google` 依赖，并在 `commands/auth.rs` 增加 host 层桥接实现：定义 Tauri 侧 auth service（可在同文件或私有子模块），实现 `feature_auth::AuthService`，内部仅委托 `GoogleAuthAdapter`（per D-01, D-04）。保持 D-03 的双类型边界：对外命令返回 runtime_tauri 需要的会话形状，不把 contracts/session 语义强行混并。</action>
  <verify>
    <automated>cargo check -p runtime_tauri</automated>
  </verify>
  <done>runtime_tauri 可编译，且 auth.rs 出现明确的 feature-auth trait 使用与 adapter-google 委托链。</done>
</task>

<task type="auto">
  <name>Task 2: 将 auth commands 收敛为薄封装并移除重复 OAuth 主体逻辑（per D-01）</name>
  <files>packages/adapters/hosts/tauri/src/commands/auth.rs, packages/adapters/hosts/tauri/src/commands/mod.rs, apps/client/native/src-tauri/src/lib.rs</files>
  <action>保留现有命令入口名（避免前端破坏），但将 `start_oauth/handle_oauth_callback/get_session` 等命令实现改为调用 Task 1 的桥接 service；删除或最小化 runtime_tauri 中 OAuth/PKCE/TCP listener/token-exchange 的重复实现，避免双实现漂移（对应 VERIFICATION gap #1）。若命令注册或模块导出需要同步调整，在 mod.rs 与 native-tauri 注册处完成最小改动。</action>
  <verify>
    <automated>cargo check -p runtime_tauri -p native-tauri && rtk rg -n "GoogleAuthAdapter|feature_auth::AuthService" packages/adapters/hosts/tauri/src/commands/auth.rs && rtk rg -n "const GOOGLE_AUTH_URL|TcpListener::bind|code_challenge_method" packages/adapters/hosts/tauri/src/commands/auth.rs</automated>
  </verify>
  <done>auth.rs 保留命令外观但不再承载完整 OAuth 细节；grep 可见 adapter/feature 接线且重复 OAuth 常量/实现显著收敛。</done>
</task>

</tasks>

<verification>
- `cargo check -p runtime_tauri -p native-tauri` 通过
- `rtk rg -n "GoogleAuthAdapter|feature_auth::AuthService" packages/adapters/hosts/tauri/src/commands/auth.rs` 命中
- `rtk rg -n "const GOOGLE_AUTH_URL|TcpListener::bind|code_challenge_method" packages/adapters/hosts/tauri/src/commands/auth.rs` 显示已移除或仅保留最小兼容封装
</verification>

<success_criteria>
- AUTH-01 的“adapter 接入闭环”被补齐
- runtime_tauri auth 不再是内联 OAuth 主实现
- auth 路径符合 “feature + adapter + host thin wrapper” 边界规则
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-feature-implementation/04-05-SUMMARY.md`
</output>
