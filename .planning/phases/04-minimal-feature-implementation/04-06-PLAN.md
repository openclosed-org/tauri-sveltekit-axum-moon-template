---
phase: 04-minimal-feature-implementation
plan: '06'
type: execute
wave: 5
depends_on:
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - packages/core/usecases/src/agent_service.rs
  - servers/api/src/routes/agent.rs
  - apps/client/web/app/src/routes/(app)/agent/+page.svelte
autonomous: true
gap_closure: true
requirements:
  - AGENT-01

must_haves:
  truths:
    - "Agent 在对话中可执行只读工具（get_counter_value/list_tenants/get_system_status）并将结果回写消息流"
    - "工具执行结果可在会话历史中持久化并在刷新后仍可见"
    - "用户可复现触发工具调用并收到结构化工具结果（满足验证缺口）"
  artifacts:
    - path: "packages/core/usecases/src/agent_service.rs"
      provides: "tool call parsing + execution + persistence"
      contains: "get_counter_value"
    - path: "packages/core/usecases/src/agent_service.rs"
      provides: "tool call parsing + execution + persistence"
      contains: "list_tenants"
    - path: "packages/core/usecases/src/agent_service.rs"
      provides: "tool call parsing + execution + persistence"
      contains: "get_system_status"
    - path: "servers/api/src/routes/agent.rs"
      provides: "agent chat route streaming tool results"
      contains: "agent/chat"
    - path: "apps/client/web/app/src/routes/(app)/agent/+page.svelte"
      provides: "UI renders streamed tool result chunks"
      contains: "data:"
  key_links:
    - from: "packages/core/usecases/src/agent_service.rs"
      to: "packages/core/usecases/src/counter_service.rs"
      via: "tool execution for get_counter_value"
      pattern: "counter.*get_value|get_counter_value"
    - from: "packages/core/usecases/src/agent_service.rs"
      to: "packages/core/usecases/src/tenant_service.rs"
      via: "tool execution for list_tenants"
      pattern: "list_tenants|tenant"
    - from: "servers/api/src/routes/agent.rs"
      to: "apps/client/web/app/src/routes/(app)/agent/+page.svelte"
      via: "SSE data lines for assistant/tool output"
      pattern: "Sse|Event::default\(\)\.data"
---

<objective>
关闭 Phase 4 的 AGENT 工具执行 gap：让 Agent 不只是“声明 tools”，而是能执行只读工具并返回可验证结果。

Purpose: 满足 04-VERIFICATION.md 的 gap #2，达成“agent 可以操作产品功能（只读）”的目标。
Output: agent_service 完整工具调用链（解析→执行→回写→持久化）+ 路由/前端可复现验证路径。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-minimal-feature-implementation/04-CONTEXT.md
@.planning/phases/04-minimal-feature-implementation/04-VERIFICATION.md
@.planning/phases/04-minimal-feature-implementation/04-03-SUMMARY.md
@.planning/phases/04-minimal-feature-implementation/04-04-SUMMARY.md
@packages/core/usecases/src/agent_service.rs
@packages/core/usecases/src/counter_service.rs
@packages/core/usecases/src/tenant_service.rs
@servers/api/src/routes/agent.rs
@apps/client/web/app/src/routes/(app)/agent/+page.svelte

<interfaces>
From packages/features/agent/src/lib.rs:
```rust
pub const AVAILABLE_TOOLS: &[(&str, &str)] = &[
    ("get_counter_value", "Get the current counter value"),
    ("list_tenants", "List all tenants"),
    ("get_system_status", "Get system health status"),
];
```

From packages/core/usecases/src/counter_service.rs:
```rust
pub trait CounterService: Send + Sync {
    async fn get_value(&self) -> Result<i64, CounterError>;
}
```

From packages/core/usecases/src/tenant_service.rs:
```rust
pub trait TenantService: Send + Sync {
    async fn list_tenants(&self) -> Result<Vec<Tenant>, TenantServiceError>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先补工具调用行为测试，再实现 agent_service 工具执行链（per D-08）</name>
  <files>packages/core/usecases/src/agent_service.rs</files>
  <behavior>
    - Test 1: 当模型返回 `get_counter_value` tool call 时，agent_service 调用 counter service 获取值，并将工具结果写入 assistant/tool 消息流
    - Test 2: 当模型返回 `list_tenants` tool call 时，agent_service 返回租户列表摘要，且消息持久化后可在 `get_messages` 读取
    - Test 3: 当模型返回 `get_system_status` tool call 时，返回系统状态对象；未知工具名时返回安全错误消息而非 panic
  </behavior>
  <action>先在 `agent_service.rs` 增加最小可执行测试（可使用本文件内测试模块与 mock port/client，不新增外部依赖），让测试先失败；再实现工具调用链：解析模型 tool_calls、执行 3 个只读工具、将结果序列化写回消息（assistant/tool），并保持现有 SSE 文本流行为。严格不实现 deferred 写操作工具（create_tenant/reset_counter）。</action>
  <verify>
    <automated>cargo test -p usecases agent_service -- --nocapture</automated>
  </verify>
  <done>agent_service 测试覆盖 3 个只读工具路径与未知工具分支，测试通过，且工具结果进入持久化消息流。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 路由与前端联动补齐“可复现工具调用”验证路径（per D-11）</name>
  <files>servers/api/src/routes/agent.rs, apps/client/web/app/src/routes/(app)/agent/+page.svelte</files>
  <behavior>
    - Test 1: POST `/api/agent/chat` 触发工具调用时，SSE 流中包含工具结果片段（非仅 assistant 自然语言）
    - Test 2: 前端流式解析后在会话中可见工具结果，刷新后通过历史接口仍可见
  </behavior>
  <action>在 `agent.rs` 保持现有接口不破坏，确保 tool 执行结果通过 SSE 下发；在 agent 页面增加最小可观察标记（如工具结果块/role 标识）以便人工和自动验证。保持 D-09 配置路径不变，不引入新设置项。</action>
  <verify>
    <automated>cargo check -p runtime_server && npm --prefix "apps/client/web/app" run check</automated>
  </verify>
  <done>路由和前端可稳定展示工具结果，且不会破坏现有聊天流式体验。</done>
</task>

</tasks>

<verification>
- `cargo test -p usecases agent_service -- --nocapture` 通过（含工具调用分支）
- `cargo check -p runtime_server` 通过
- `npm --prefix "apps/client/web/app" run check` 通过
- `rtk rg -n "get_counter_value|list_tenants|get_system_status" packages/core/usecases/src/agent_service.rs` 命中执行代码路径（不只是常量声明）
</verification>

<success_criteria>
- Agent 工具调用从“声明”变为“可执行 + 可持久化 + 可观察”
- 3 个只读工具均有可复现验证路径
- 不引入 deferred 的写操作工具，范围严格收敛
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-feature-implementation/04-06-SUMMARY.md`
</output>
