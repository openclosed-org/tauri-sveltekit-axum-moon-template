// Retired in Phase 14-05.
// Legacy WDIO desktop E2E stack no longer participates in repository or CI gates.
// Active desktop coverage: e2e-desktop-playwright (tauri-playwright).
export const config = {
	retired: true,
	note: 'Use e2e-desktop-playwright/package.json scripts instead.'
};
