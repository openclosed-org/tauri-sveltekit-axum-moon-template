projects:
  # Root project (this repo)
  repo: '.'
  # Default backend projects. Optional app shells are invoked by explicit just lanes.
  kernel: 'packages/kernel'
  contracts-api: 'packages/contracts/api'

vcs:
  client: 'git'
  defaultBranch: 'main'
