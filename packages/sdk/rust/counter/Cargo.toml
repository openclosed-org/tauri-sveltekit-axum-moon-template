[package]
name = "sdk-counter"
version = "0.1.0"
edition = "2024"
description = "SDK Counter — client-side counter interface (trait + types)"
license = "Apache-2.0"
rust-version.workspace = true
publish = false

[dependencies]
async-trait = { workspace = true }
kernel = { workspace = true }
thiserror = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }

[lints]
workspace = true
