# ops — Operations

All operational tasks through `just` commands. No hand-written scripts.

## Structure
- `migrations/runner/` — Database migration CLI
- `observability/` — OpenTelemetry + Vector config
- `runbooks/` — Incident response manuals
- `scripts/` — Bootstrap + health check scripts
