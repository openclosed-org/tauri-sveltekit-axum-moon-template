name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    name: Quality Gates (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libglib2.0-dev \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Toolchain bootstrap complete
        run: echo "CI environment ready on ${{ runner.os }}"

      - name: Rust check
        run: cargo check --workspace --exclude native-tauri

      - name: Rust fmt check
        run: cargo fmt --all -- --check

      - name: Rust clippy
        run: cargo clippy --workspace --exclude native-tauri -- -D warnings

      - name: Rust test
        run: cargo test --workspace --exclude native-tauri

      - name: Install frontend deps
        working-directory: apps/web
        run: bun install --frozen-lockfile

      - name: Frontend check
        working-directory: apps/web
        run: bun run check

      - name: Frontend lint
        working-directory: apps/web
        run: bun run lint

      - name: Frontend unit tests
        working-directory: apps/web
        run: bun run test:unit

      - name: Install Playwright browsers
        working-directory: apps/web
        run: npx playwright install --with-deps chromium

      - name: Frontend E2E tests
        working-directory: apps/web
        run: bun run test:e2e --project=desktop

      - name: Frontend build
        working-directory: apps/web
        run: bun run build

      - name: Tauri crate build verification
        run: cargo build --manifest-path apps/desktop/src-tauri/Cargo.toml
