-- Admin service migrations
-- Admin dashboard uses aggregation queries across existing tables
-- No dedicated admin tables needed (reads from tenant, counter, user tables)
-- This migration is a no-op placeholder for consistency

SELECT 1;
