# Counter — 黄金示例模块 🏆

> 简单计数器功能，按租户隔离。展示完整 Clean Architecture 四层结构、单元测试、集成测试、SQL 迁移。
> **新增业务模块时复制此目录。**

```bash
cargo test -p counter-service
cargo build -p counter-service
```

详细架构说明见 [services/README.md](../README.md)。
