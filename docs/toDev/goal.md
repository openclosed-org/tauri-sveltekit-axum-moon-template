我要继续迭代我当前的项目,目前项目选型是 tauri2+sveltekit+axum+libsql/turso+surrealDB + moon (大框架) , 我要做一个 boilerplate, 同时我要达到 至少是 注册可用google 一键登录,同时 多租户,同时 后端的 容器 反代 类redis缓存 数据库都要 也配置好 测试通;
基建方面 需要 本地代码库索引mcp + websearch mcp + rearch mcp + 各个领域(前端+后端+tauri+测试)skills mcp,
目前UIUX方面的 技术选型 是 sveltekit + bitsUI + tailwindcssV4 + lucide-animated + vitepress + lottieplayer 等等(我需要尽可能完善 package.json 同时把后续可能需要的 工具项目和依赖都尽量补齐,但是注释掉没用到的) , UIUX端要做到 mobilefirst, 同时各个平台(包括移动端)都能响应式适配, 我只有三个页面 一个登录,一个 counter计数器,一个管理端web
目前tauri (客户端)和 axum(云服务器端) 方面 的 cargo.toml 也是同理, cargo里面需要深度查阅 tauri 和 axum的docs和 awesome tauri/axum 项目的列表,插件 生态文档现状,然后查看可能需要的各种领域需要引入的依赖库工具库,没用到的暂时注释掉 , 目前只需要跑通 编译成功 测试通就行.
要求符合工程最佳实践,提前预判项目发展到 中型可持续增长阶段的时候,需要工具项目,同时要以26年3月28日为基准,尽量联网核实 exa mcp 搜索 数据,参考
