# Tauri RPC Contract Docs

> 状态：按需参考，不是默认后端上下文。
>
> 真理源：桌面端命令实现、调用代码与共享 contracts，而不是这份手工文档。

## 1. 这份文档负责什么

这里只保留 Tauri RPC 文档的边界说明，不再手工维护完整命令目录。

## 2. 当前真理源

理解 Tauri command / RPC 形状时，优先看：

1. 桌面端真实命令实现
2. 前端调用代码
3. 若有共享 DTO，则看 `packages/contracts/**`

## 3. 为什么收口

旧版 Tauri command 手册的问题是：

1. 默认上下文当前并不以桌面壳层为主。
2. 手工维护命令列表非常容易和真实实现脱节。
3. 它对当前后端主链帮助有限，不适合继续占据高优先级文档位置。

## 4. 当前建议用法

只有在任务明确涉及桌面端 RPC 时，再进入本目录，并优先以代码为准。

## 5. 一句话结论

Tauri RPC 是按需阅读的局部协议面，不再通过长篇手工文档维持“完整参考”。
